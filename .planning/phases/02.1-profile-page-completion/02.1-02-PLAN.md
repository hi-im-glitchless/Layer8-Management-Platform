---
phase: 02.1-profile-page-completion
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - frontend/src/routes/Profile.tsx
  - frontend/src/features/profile/api.ts
  - frontend/src/features/profile/hooks.ts
  - frontend/src/components/layout/Header.tsx
autonomous: false

must_haves:
  truths:
    - "User sees their avatar (or initials fallback with cool blue accent) and display name on the profile page"
    - "User can click Edit Profile, change display name, upload avatar, and save — toast confirms success"
    - "User can open Change Password dialog and change password using existing PasswordChange component"
    - "User can open Regenerate TOTP dialog and re-setup 2FA using existing TOTPSetup component"
    - "Profile page shows account metadata: Member since date, last login, and role badge"
    - "Header dropdown shows avatar image (or initials) and display name when available"
  artifacts:
    - path: "frontend/src/routes/Profile.tsx"
      provides: "Complete profile page with view/edit toggle, avatar, security settings"
      min_lines: 150
    - path: "frontend/src/features/profile/api.ts"
      provides: "Profile API functions (updateProfile, uploadAvatar, deleteAvatar)"
      exports: ["updateProfile", "uploadAvatar", "deleteAvatar"]
    - path: "frontend/src/features/profile/hooks.ts"
      provides: "TanStack Query hooks for profile mutations"
      exports: ["useUpdateProfile", "useUploadAvatar", "useDeleteAvatar"]
    - path: "frontend/src/components/layout/Header.tsx"
      provides: "Header with avatar image and display name"
      contains: "AvatarImage"
  key_links:
    - from: "frontend/src/features/profile/api.ts"
      to: "/api/profile"
      via: "apiClient and apiUpload calls"
      pattern: "api/profile"
    - from: "frontend/src/features/profile/hooks.ts"
      to: "frontend/src/features/profile/api.ts"
      via: "TanStack Query mutation wrappers"
      pattern: "useMutation"
    - from: "frontend/src/routes/Profile.tsx"
      to: "frontend/src/features/profile/hooks.ts"
      via: "Hook consumption in component"
      pattern: "useUpdateProfile|useUploadAvatar"
    - from: "frontend/src/routes/Profile.tsx"
      to: "frontend/src/features/auth/hooks.ts"
      via: "useAuth for current user data"
      pattern: "useAuth"
    - from: "frontend/src/components/layout/Header.tsx"
      to: "frontend/src/features/auth/hooks.ts"
      via: "useAuth for avatar and displayName"
      pattern: "user\\?.avatarUrl|user\\?.displayName"
---

<objective>
Build the complete profile page UI with view/edit toggle, avatar upload with initials fallback, security settings dialogs (password change, TOTP regeneration), account metadata display, and update the header to show avatar and display name.

Purpose: This is the user-facing deliverable of Phase 02.1. Users need to manage their profile, change passwords, regenerate TOTP, and see their identity reflected in the header.

Output: Fully functional profile page replacing skeleton UI, updated header with avatar/displayName, profile API hooks and functions.
</objective>

<execution_context>
@/home/glitchless/.claude/get-shit-done/workflows/execute-plan.md
@/home/glitchless/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-profile-page-completion/02.1-CONTEXT.md
@.planning/phases/02.1-profile-page-completion/02.1-RESEARCH.md
@.planning/phases/02.1-profile-page-completion/02.1-01-SUMMARY.md

@frontend/src/routes/Profile.tsx
@frontend/src/components/layout/Header.tsx
@frontend/src/features/auth/hooks.ts
@frontend/src/features/auth/api.ts
@frontend/src/features/auth/types.ts
@frontend/src/lib/api.ts
@frontend/src/components/auth/PasswordChange.tsx
@frontend/src/components/auth/TOTPSetup.tsx
@frontend/src/components/ui/avatar.tsx
@frontend/src/components/ui/card.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profile API layer and build complete profile page</name>
  <files>
    frontend/src/features/profile/api.ts
    frontend/src/features/profile/hooks.ts
    frontend/src/routes/Profile.tsx
  </files>
  <action>
1. Create `frontend/src/features/profile/api.ts`:
   - `updateProfile(data: { displayName?: string }): Promise<User>` — calls `apiClient<User>('/api/profile', { method: 'PUT', body: JSON.stringify(data) })`
   - `uploadAvatar(formData: FormData): Promise<{ avatarUrl: string }>` — calls `apiUpload<{ avatarUrl: string }>('/api/profile/avatar', formData)`
   - `deleteAvatar(): Promise<{ success: boolean }>` — calls `apiClient<{ success: boolean }>('/api/profile/avatar', { method: 'DELETE' })`
   - Import `apiClient` and `apiUpload` from `@/lib/api`, import `User` from `@/features/auth/types`

2. Create `frontend/src/features/profile/hooks.ts`:
   - `useUpdateProfile()`: useMutation wrapping `updateProfile`, on success invalidate `['auth', 'me']` query key
   - `useUploadAvatar()`: useMutation wrapping `uploadAvatar`, on success invalidate `['auth', 'me']` query key, show `toast.error` on failure
   - `useDeleteAvatar()`: useMutation wrapping `deleteAvatar`, on success invalidate `['auth', 'me']` query key
   - Import from `@tanstack/react-query`, `sonner` for toast, and the api functions

3. Rewrite `frontend/src/routes/Profile.tsx` completely (replace skeleton):

   **Layout:** One scrollable page. Page title "Profile" with subtitle at top. Below: Account Info section, then Security Settings section.

   **Account Info section** — Single Card containing:
   - Left area: Large avatar (h-24 w-24) using Radix Avatar with AvatarImage (if avatarUrl exists) and AvatarFallback (initials in cool blue: `bg-[oklch(0.6_0.15_250)] text-white text-2xl`). Initials logic: if displayName exists, use first letter of each word (max 2 chars); otherwise use first letter of username.
   - In edit mode: camera/upload icon overlay on avatar (hidden file input triggered by clicking the avatar). Use Lucide `Camera` icon. File input accepts `image/jpeg,image/png,image/gif`. Client-side validation: max 2MB, valid image type. On change, call uploadAvatar mutation.
   - Display name field: In view mode shows text (or "Not set" in muted). In edit mode shows Input with maxLength 50 and placeholder "Enter display name".
   - Username field: Always read-only text display (not editable).
   - Account metadata: Show "Member since {formatDate(createdAt)}" (use date-fns `format` with "MMMM yyyy" pattern), "Last login: {formatRelative(lastLoginAt)}" (use date-fns `formatDistanceToNow` with `{ addSuffix: true }`), and role Badge (Admin or User).
   - View/edit toggle: "Edit Profile" button in view mode. "Save" and "Cancel" buttons in edit mode.
   - Use React Hook Form for displayName field with Zod validation (maxLength 50).
   - On save: call updateProfile mutation, on success toast "Profile updated" and exit edit mode.
   - On cancel: reset form and exit edit mode.

   **Security Settings section** — Single Card below Account Info:
   - Two rows, each a bordered rounded div with flex layout (label left, button right):
     Row 1: "Change Password" with description "Update your account password" and Button "Change Password" that opens PasswordChange dialog.
     Row 2: "Two-Factor Authentication" with description showing TOTP status ("Enabled" with green dot indicator) and Button "Regenerate" that opens TOTP regeneration dialog.
   - Password Change Dialog: Use Radix Dialog wrapping the existing PasswordChange component. The PasswordChange component needs `currentPassword` when `mustResetPassword` is false — add a current password field ABOVE the existing component, or modify the dialog to include it. Actually, looking at the existing PasswordChange component, it only has newPassword and confirmPassword fields. For the profile page use case (not onboarding), the user must provide their current password first. Implement this in the dialog:
     - Add a "Current Password" input field at the top of the dialog
     - Below it, render the PasswordChange component
     - Override onSubmit to call `changePassword({ newPassword, currentPassword })` with both values
     - On success: toast "Password changed successfully", close dialog
   - TOTP Regeneration Dialog: Use Radix Dialog. Show warning text: "This will invalidate your current authenticator app setup. You will need to scan a new QR code." with a "Continue" button. On continue, call setupTOTP to get QR code, then render TOTPSetup component for verification. On success: toast "Two-factor authentication regenerated", close dialog, invalidate auth query.
     - IMPORTANT: The existing TOTP setup endpoint (`/api/auth/totp/setup`) returns 400 if `totpEnabled` is already true. For regeneration, either: (a) create a new backend endpoint, or (b) have the dialog call the existing setup endpoint which needs to be modified to allow re-setup. Since Plan 01 created the backend, check the 02.1-01-SUMMARY for how this was handled. If the setup endpoint still rejects when totpEnabled=true, the regeneration dialog should call a new endpoint. For now, implement the UI assuming an endpoint exists that generates a new TOTP secret even when totpEnabled=true. Add a note in the dialog component about this. The simplest approach: modify the /api/auth/totp/setup endpoint in auth.ts to allow re-setup when called by an authenticated user (remove the `if (user.totpEnabled) return 400` check, or add a `regenerate` query param).

4. Component state management:
   - `const [isEditing, setIsEditing] = useState(false)`
   - `const [passwordDialogOpen, setPasswordDialogOpen] = useState(false)`
   - `const [totpDialogOpen, setTotpDialogOpen] = useState(false)`
   - Use `useAuth()` hook for current user data
   - Use `useForm` from react-hook-form for the displayName field

5. All imports needed: React useState, useForm + zodResolver, useAuth, useChangePassword, useSetupTOTP, useVerifyTOTPSetup from auth hooks, profile hooks (useUpdateProfile, useUploadAvatar), Card/CardContent/CardHeader/CardTitle/CardDescription, Avatar/AvatarImage/AvatarFallback, Badge, Button, Input, Label, Dialog/DialogContent/DialogHeader/DialogTitle/DialogDescription, PasswordChange component, TOTPSetup component, toast from sonner, format/formatDistanceToNow from date-fns, Camera/Loader2/Shield/Check icons from lucide-react.
  </action>
  <verify>
- `cd /home/glitchless/Documents/Layer8/frontend && npx tsc --noEmit` compiles with no errors
- `npm run build` (Vite build) succeeds
- Profile page renders at /profile without crashes (start dev server, navigate to /profile)
  </verify>
  <done>
Profile page shows avatar with initials fallback (cool blue accent), display name, username, account metadata (member since, last login, role). Edit mode allows changing display name and uploading avatar. Security section has working Change Password and Regenerate TOTP dialogs. All mutations invalidate auth cache for immediate UI updates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update header to show avatar and display name</name>
  <files>
    frontend/src/components/layout/Header.tsx
  </files>
  <action>
1. Update `frontend/src/components/layout/Header.tsx`:
   - Import `AvatarImage` from `@/components/ui/avatar` (currently only AvatarFallback is imported)
   - Update the initials logic: If user has displayName, use first letter of each word (max 2 chars, uppercased). If no displayName, use first letter of username.
   - In the Avatar inside DropdownMenuTrigger: Add `<AvatarImage src={user?.avatarUrl} alt={user?.displayName || user?.username || 'User'} />` before AvatarFallback. Radix Avatar handles the fallback automatically when image fails to load or src is null.
   - In the DropdownMenuLabel section: Show display name as primary text if available, username below it as secondary. If no display name, show username as primary text. Keep the Admin badge and MFA status.
   - Updated structure for DropdownMenuLabel:
     ```
     <p className="text-sm font-medium">{user?.displayName || user?.username || 'User'}</p>
     {user?.displayName && (
       <p className="text-xs text-muted-foreground">@{user?.username}</p>
     )}
     ```
     Keep the existing Admin badge and MFA status line.

2. No changes to sidebar — per locked decision, sidebar keeps username only.
  </action>
  <verify>
- `cd /home/glitchless/Documents/Layer8/frontend && npx tsc --noEmit` compiles with no errors
- Header dropdown shows avatar image when user has one, initials when they don't
- Display name appears in dropdown label when set
  </verify>
  <done>
Header avatar shows uploaded image or initials fallback. Dropdown label shows display name (with @username below) when display name is set, falls back to username when not set. Sidebar unchanged (uses username per locked decision).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete profile page</name>
  <files>none</files>
  <action>
Human verification of the complete profile page implementation. All code was built in Tasks 1-2.
  </action>
  <verify>
1. Start backend (`cd backend && npx tsx src/index.ts`) and frontend (`cd frontend && npm run dev`)
2. Log in and navigate to /profile
3. Verify profile page layout:
   - Avatar with initials fallback (cool blue circle with letter)
   - Username displayed (read-only)
   - Display name shows "Not set" initially
   - Account metadata: "Member since" date, "Last login" time, role badge
4. Click "Edit Profile":
   - Display name field becomes editable input
   - Avatar shows camera overlay icon
   - Save and Cancel buttons appear
5. Type a display name, click Save:
   - Toast "Profile updated" appears
   - Fields return to view mode showing new display name
   - Avatar initials update to reflect display name
6. Click Edit again, click the avatar to upload an image:
   - File picker opens (only images accepted)
   - After selecting image, avatar updates to show the image
7. Verify header: Click user menu in top right — should show avatar image and display name
8. Click "Change Password" in Security section:
   - Dialog opens with current password field and new/confirm password fields
   - Can submit a password change
9. Click "Regenerate" on TOTP row:
   - Dialog opens with warning about invalidating current authenticator
   - Can proceed to see QR code and verify with authenticator app
  </verify>
  <done>User confirms profile page works correctly: avatar, display name editing, security dialogs, account metadata, and header integration all function as expected.</done>
</task>

</tasks>

<verification>
1. Profile page loads at /profile with real user data (no skeleton components)
2. View mode: avatar, username, display name (or "Not set"), metadata all visible
3. Edit mode: display name editable, avatar uploadable, save/cancel functional
4. Password change dialog opens and functions correctly (requires current password)
5. TOTP regeneration dialog opens with warning and QR code flow
6. Header shows avatar and display name in dropdown
7. All toast notifications fire on success/error
8. No TypeScript errors: `npx tsc --noEmit` passes in both frontend and backend
</verification>

<success_criteria>
- Profile page fully replaces skeleton with working UI
- Avatar displays uploaded image or initials fallback with cool blue accent
- Display name editable via view/edit toggle with toast feedback
- Password change works via modal dialog with current password requirement
- TOTP regeneration works via modal dialog with QR code
- Account metadata shows join date, last login, and role
- Header shows avatar and display name
- No regressions to existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-profile-page-completion/02.1-02-SUMMARY.md`
</output>
