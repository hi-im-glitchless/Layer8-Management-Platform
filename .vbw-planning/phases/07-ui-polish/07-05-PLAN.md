---
phase: "07"
plan: "07-05"
title: "AuditLog Header, Dead Code Cleanup & Final Polish"
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04"]
cross_phase_deps: []
skills_used: []
must_haves:
  - "AuditLog.tsx renders page title 'Audit Log' and description 'View security and compliance audit trail' above AuditLogViewer"
  - "frontend/src/App.css does not exist (deleted)"
  - "grep -r 'import.*App.css' frontend/src/ returns zero matches"
  - "No unused imports in files modified during Phase 07 (verified via TypeScript build)"
  - "npm run build in frontend/ succeeds with zero errors"
---

## Objective

Add the missing AuditLog page header for layout consistency, delete the unused App.css Vite boilerplate, clean up any dead imports introduced or discovered during the theme migration, and perform a final verification pass to confirm the phase acceptance criteria are met.

## Context

- `@frontend/src/routes/AuditLog.tsx` -- minimal wrapper that renders `<AuditLogViewer adminMode={false} />` with no page title or description; inconsistent with Profile.tsx and other page patterns
- `@frontend/src/App.css` -- Vite boilerplate CSS (logo spin animation, .card padding, .read-the-docs color), confirmed unused (no imports found)
- Page header pattern from Profile.tsx: `<h1 className="text-3xl font-bold tracking-tight">` + `<p className="text-muted-foreground mt-2">`
- All prior Phase 07 plans will have modified 20+ component files; unused imports may have been introduced or discovered

## Tasks

### Task 1: Add page header to AuditLog.tsx

**Scope:** `frontend/src/routes/AuditLog.tsx`

**Action:**
- Add a header section before `<AuditLogViewer>`, matching the Profile.tsx pattern:
  ```tsx
  <div>
    <h1 className="text-3xl font-bold tracking-tight">Audit Log</h1>
    <p className="text-muted-foreground mt-2">
      View security and compliance audit trail
    </p>
  </div>
  ```
- The existing `space-y-6` wrapper already provides proper spacing

**Acceptance:** AuditLog page shows "Audit Log" heading and "View security and compliance audit trail" description above the log viewer table. Visual layout matches Profile page pattern.

### Task 2: Delete App.css

**Scope:** `frontend/src/App.css`

**Action:**
- Delete the file `frontend/src/App.css`
- Verify no import references exist (already confirmed: `grep -r 'import.*App.css' frontend/src/` returns zero matches)

**Acceptance:** `frontend/src/App.css` no longer exists. Build succeeds without it.

### Task 3: Remove unused imports and dead code

**Scope:** All files modified in Phase 07

**Action:**
- Run `npx tsc --noEmit` from `frontend/` to identify any TypeScript errors or unused import warnings
- Check each file modified in Plans 07-01 through 07-04 for:
  - Unused imports (e.g., if a lucide icon was replaced with a theme class and its import is no longer needed)
  - Unreachable code or dead variables
- Remove any discovered dead code
- Do NOT add new imports, comments, or code beyond cleanup

**Acceptance:** TypeScript compilation succeeds with no errors related to Phase 07 changes. No unused imports remain in modified files.

### Task 4: Final build verification and visual sanity check

**Scope:** Full frontend build

**Action:**
- Run `npm run build` in `frontend/` to verify the production build succeeds
- Verify all acceptance criteria from the phase context:
  1. Dashboard shows real session data and quick-action cards
  2. All visible "Layer8" text replaced with "AI Template Regenerator"
  3. Zero hardcoded Tailwind color classes in components that have theme equivalents
  4. App.css deleted, no dead imports
  5. AuditLog page has proper header
  6. OnboardingWizard uses theme colors
- Run a grep sweep: `grep -rn 'bg-green-500\|bg-red-500\|bg-blue-500\|bg-yellow-500\|bg-orange-500\|border-blue-500\|text-blue-500' frontend/src/` to confirm zero hardcoded named colors remain (excluding button.tsx gradient variants and Login.tsx atmospheric styles which are intentionally exempt)

**Acceptance:** Production build succeeds. All phase acceptance criteria verified. Any remaining hardcoded colors are documented as intentional exemptions (Login.tsx atmospheric backgrounds, button.tsx gradients for always-dark contexts).
