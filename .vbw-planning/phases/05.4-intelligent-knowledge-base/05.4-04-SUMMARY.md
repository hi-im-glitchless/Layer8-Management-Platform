---
phase: "05.4"
plan: "05.4-04"
title: "Frontend Mapping Table UX"
status: complete
tasks_completed: 5
commits:
  - hash: cb6b951
    message: "feat(adapter-ui): create GwFieldAutocomplete component with typeahead and Jinja2 generation"
  - hash: 6136e5b
    message: "feat(adapter-ui): make MappingTable fully editable with delete and GW autocomplete"
  - hash: 3d169d0
    message: "refactor(adapter-ui): replace correction chat with editable mapping table in StepVerify"
  - hash: 86b42f8
    message: "feat(adapter-ui): implement bidirectional table-PDF sync (click row scrolls PDF, highlight adds row)"
  - hash: cbd6c3a
    message: "refactor(adapter): clean up chat-related types and simplify selection state for table workflow"
---

## What Was Built

Replaced the correction chat UI with a direct editable mapping table. All auto-mapped entries are pre-filled as editable rows with GW field autocomplete, delete buttons, and bidirectional PDF sync.

1. **GwFieldAutocomplete Component** (cb6b951) -- New autocomplete with FIELD_MARKER_MAP data, fuzzy match, Jinja2 generation, keyboard nav
2. **MappingTable Full Editability** (6136e5b) -- Removed gaps, all rows editable via autocomplete, delete button per row, Add Row button
3. **Replace Chat with Mapping Table** (3d169d0) -- Removed entire chat panel, wired editable MappingTable in right column, Regenerate button
4. **Bidirectional Table-PDF Sync** (86b42f8) -- Row click scrolls PDF, PDF highlight adds table row, pulse animation, keyboard a11y
5. **Cleanup & Polish** (cbd6c3a) -- Simplified useSelectionState, removed dead exports, kept SSE types for backward compat

## Files Modified

- `frontend/src/features/adapter/components/GwFieldAutocomplete.tsx` -- NEW: autocomplete with 23 GW fields, fuzzy search, Jinja2 template generation
- `frontend/src/features/adapter/components/MappingTable.tsx` -- Rewritten: removed gaps/ParagraphPicker, all rows editable, delete, onRowClick, highlightedIndex
- `frontend/src/features/adapter/components/StepVerify.tsx` -- Rewritten: chat panel replaced with mapping table, bidirectional sync, Regenerate button, kept KB stats/correction logic
- `frontend/src/features/adapter/hooks.ts` -- Simplified useSelectionState (removed confirm/reject), added deprecation note
- `frontend/src/features/adapter/index.ts` -- Removed useAdapterChat from barrel export

## Deviations

- Preserved kbStatsQuery and correctionUpdate logic from Plan 05.4-03 that was already in StepVerify (not in original plan context but discovered during implementation)
- MappingOverlayCard.tsx retained (InteractivePdfViewer still imports it for selection overlays; plan said "remove if replaced" -- it is not fully replaced)
- ChatPanel.tsx left as dead code (not listed in plan's file modifications; could be deleted in future cleanup)
