---
phase: "06.1"
plan_count: 5
status: issues_found
started: "2026-02-16"
completed: "2026-02-16"
total_tests: 11
passed: 2
skipped: 8
issues: 1
---

# Phase 6.1: Executive Report HTML Overhaul -- UAT

## PA-T1: HTML skeleton renders in browser (English)
**Plan:** 06.1-A -- HTML Skeleton, CSS Foundation & DOCX-to-HTML Conversion
**Scenario:** Open `test-templates/executive/skeleton-en.html` in a browser. Verify all 12 section headings render, Chart.js canvas placeholders are visible, and the cover page shows metadata placeholders ([CLIENT_NAME], [PROJECT_CODE], etc.) with CONFIDENCIAL badge.
**Expected:** All 12 sections present with headings, 6 chart canvases visible, cover page with red branding and Roboto font.
**Result:** PASS

## PA-T2: Portuguese skeleton matches English structure
**Plan:** 06.1-A -- HTML Skeleton, CSS Foundation & DOCX-to-HTML Conversion
**Scenario:** Open `test-templates/executive/skeleton-pt-pt.html` in a browser. Verify same section structure as English but all visible text is in Portuguese (e.g. "Sumario Executivo", "Metricas Principais", "CONFIDENCIAL").
**Expected:** Same 12 data-section keys, same chart canvases, all labels in Portuguese.
**Result:** PASS

## PB-T1: DOCX upload returns sanitized HTML and entity mappings
**Plan:** 06.1-B -- HTML Sanitization Pipeline & Session Model Overhaul
**Scenario:** Upload a test DOCX file (e.g. tech-report.docx) through the wizard. After upload completes, verify the session contains sanitizedHtml with entity span tags and an entityMappings array with detected entities.
**Expected:** Upload auto-triggers full pipeline. Session has sanitizedHtml (HTML with `<span class="entity ...">` tags) and entityMappings array with originalValue, placeholder, entityType fields.
**Result:** ISSUE -- report upload failed (pipeline failed). Severity: critical.

## PB-T2: Consistent incrementing placeholders
**Plan:** 06.1-B -- HTML Sanitization Pipeline & Session Model Overhaul
**Scenario:** In the sanitized HTML, find an entity that appears multiple times (e.g. the same person name). Verify each occurrence uses the same placeholder (e.g. all "John Doe" instances become [PERSON_1]).
**Expected:** Same original value always maps to the same placeholder. Different values of the same type get incrementing numbers.
**Result:** SKIP -- blocked by PB-T1 (upload pipeline failure)

## PC-T1: Sanitize Review shows HTML preview with entity highlights
**Plan:** 06.1-C -- Frontend Sanitize Review
**Scenario:** After uploading a DOCX, navigate to the Sanitize & Review step (Step 2). Verify the HTML preview renders in an iframe with colored entity highlights visible in the text.
**Expected:** Sandboxed iframe showing sanitized HTML with entity spans highlighted by type (different colors per entity type).
**Result:** SKIP -- blocked by PB-T1 (upload pipeline failure)

## PC-T2: Mapping table toggle and entity operations
**Plan:** 06.1-C -- Frontend Sanitize Review
**Scenario:** In the Sanitize Review step, click the "Show Mapping Table" toggle. Verify a 60/40 split layout appears with the entity mapping table on the right showing 4 columns (Original Value, Placeholder, Entity Type, Actions). Try changing an entity type via the dropdown.
**Expected:** 60/40 split layout, 4-column table with all detected entities, entity type dropdown works.
**Result:** SKIP -- blocked by PB-T1 (upload pipeline failure)

## PC-T3: Text selection creates entity via popover
**Plan:** 06.1-C -- Frontend Sanitize Review
**Scenario:** In the HTML preview iframe, select a piece of text that was not auto-detected as an entity. Verify a floating popover appears with an entity type dropdown and "Add Mapping" button. Add the mapping and verify it appears in the table.
**Expected:** Popover appears near selection, allows choosing entity type, new mapping appears in table with isManual badge.
**Result:** SKIP -- blocked by PB-T1 (upload pipeline failure)

## PD-T1: Generation pipeline progress stages
**Plan:** 06.1-D -- LLM HTML Generation, Chart.js Data & Narrative Prompt
**Scenario:** From Step 3 (Generate), click the generate button. Watch the progress display as SSE events arrive. Verify stages progress through: computing -> chart_data -> narrative -> assembling_html -> converting_pdf.
**Expected:** All 5 stages display with correct labels ("Computing risk metrics...", "Preparing chart data...", "Generating HTML report sections...", "Assembling HTML report...", "Converting to PDF...").
**Result:** SKIP -- blocked by PB-T1 (upload pipeline failure)

## PE-T1: De-sanitize toggle in Review step
**Plan:** 06.1-E -- Review Step, De-sanitize Toggle, PDF Download & Cleanup
**Scenario:** After generation completes, go to the Review step (Step 4). Toggle the "Show Real Values" button. Verify that entity placeholders like [PERSON_1] are replaced with the original values in the HTML preview. Toggle back to verify sanitized view returns.
**Expected:** Toggle ON shows real values (e.g. actual names/IPs), toggle OFF shows placeholders. Both states render correctly in the iframe.
**Result:** SKIP -- blocked by PB-T1 (upload pipeline failure)

## PE-T2: PDF download from de-sanitized HTML
**Plan:** 06.1-E -- Review Step, De-sanitize Toggle, PDF Download & Cleanup
**Scenario:** In the Download step (Step 5), click the "Download PDF" button. Verify a PDF file downloads with correct formatting (A4 pages, Roboto font, charts rendered, real values shown).
**Expected:** PDF downloads with correct filename, A4 format, brand colors, charts visible, entity placeholders replaced with real values.
**Result:** SKIP -- blocked by PB-T1 (upload pipeline failure)

## PE-T3: No obsolete code remains (build verification)
**Plan:** 06.1-E -- Review Step, De-sanitize Toggle, PDF Download & Cleanup
**Scenario:** Run `npm run build` in both backend/ and frontend/ directories. Verify both compile without errors. Check that no references to SanitizationDiffView, DenyListEditor, chart_renderer.py, or reportDocxPath remain.
**Expected:** Both builds succeed. No dead imports or references to obsolete Phase 6 code.
**Result:** SKIP -- blocked by PB-T1 (upload pipeline failure)
