---
phase: "05.6"
plan: "05.6-01"
status: complete
---

## Tasks Completed
- Task 1: Create zone backfill and dead entry prune script -- c2d4cd6
- Task 2: Update Prisma schema with zone in unique constraint + migration SQL -- ff63821
- Task 3: Update upsertMapping/bulkUpsertMappings where clauses to 5-field compound key -- 317b051
- Task 4: Update all remaining Prisma unique key references (CorrectionEntry, persistMappingsToKB, tests) -- 7b66a9a

## Files Modified
- backend/prisma/backfill-zones.ts (NEW)
- backend/prisma/schema.prisma
- backend/prisma/migrations/20260215000000_add_zone_to_unique_constraint/migration.sql (NEW)
- backend/src/services/templateMapping.ts
- backend/src/services/templateAdapter.ts
- backend/src/services/__tests__/templateMapping.test.ts

## Deviations
- Prisma migration created manually (migration SQL + directory) instead of via `npx prisma migrate dev` because the environment is non-interactive. The migration SQL is equivalent and can be applied with `npx prisma migrate deploy`.
- Used `z.input<typeof templateMappingSchema>` instead of `z.infer<>` for TemplateMappingInput to preserve backward compatibility (callers can omit zone, Zod default fills 'body'). Plan specified `z.string().min(1).default('body')` which was applied, but `z.infer` would have made zone required in the TypeScript type, breaking existing callers.

## Verification
- Schema has zone in unique constraint: `@@unique([templateType, language, normalizedSectionText, gwField, zone])` at line 106
- Backfill script exists: `backend/prisma/backfill-zones.ts` OK
- Old compound key no longer used in backend/src/: 0 references
- New compound key used in templateMapping.ts: 2 occurrences (upsertMapping + bulkUpsertMappings)
- TypeScript compiles: 0 errors in templateMapping.ts and templateMapping.test.ts (pre-existing errors in other files unrelated to this plan)
