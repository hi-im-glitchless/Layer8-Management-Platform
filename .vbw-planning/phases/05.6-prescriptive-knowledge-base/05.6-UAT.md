---
phase: "05.6"
plan_count: 5
status: in_progress
started: 2026-02-15
total_tests: 10
passed: 0
skipped: 0
issues: 0
---

## P01-T1: Backfill Script Exists and Is Runnable

**Plan:** 05.6-01 -- Schema Migration: Zone Unique Constraint, Backfill & Prune
**Scenario:** Open `backend/prisma/backfill-zones.ts` and verify it contains zone inference logic (reading BlueprintPattern data, field-based heuristics, deduplication handling) and dead entry pruning (confidence < 0.3 deletion).
**Expected:** Script exists, reads clearly, and has console.log output for each backfill action.
**Result:**

## P01-T2: Migration SQL Drops Old and Creates New Index

**Plan:** 05.6-01 -- Schema Migration: Zone Unique Constraint, Backfill & Prune
**Scenario:** Open `backend/prisma/migrations/20260215000000_add_zone_to_unique_constraint/migration.sql` and verify it drops the old 4-field unique index and creates a new 5-field index that includes `zone`.
**Expected:** SQL contains DROP INDEX on the old constraint and CREATE UNIQUE INDEX with all 5 fields (templateType, language, normalizedSectionText, gwField, zone).
**Result:**

## P02-T1: prescriptiveLookup Returns Locked/Unknown Split

**Plan:** 05.6-02 -- Backend Prescriptive Lookup & Cross-Type Fallback Removal
**Scenario:** Open `backend/src/services/templateMapping.ts` and find the `prescriptiveLookup()` function. Verify it fetches high-confidence entries (>= 0.8), builds a lookup map keyed by `normalizedText::zone`, and returns `{ locked, unknown }`.
**Expected:** Function is exported, accepts `(sections, templateType, language, lockThreshold?)`, and returns `PrescriptiveLookupResult`.
**Result:**

## P02-T2: Cross-Type Fallback Is Gone

**Plan:** 05.6-02 -- Backend Prescriptive Lookup & Cross-Type Fallback Removal
**Scenario:** Run `grep -n "otherTypes\|cross.type\|0\.7x" backend/src/services/templateAdapter.ts` and verify there are no references to the old cross-type fallback code that queried other template types with a 0.7x penalty.
**Expected:** Zero matches (or only a backward-compat `is_cross_type_fallback: false` literal, no actual fallback logic).
**Result:**

## P03-T1: Inline Annotations in Document Structure Prompt

**Plan:** 05.6-03 -- Python Annotated Prompt with Inline Resolved/Needs-Mapping Annotations
**Scenario:** Open `sanitization-service/app/services/analysis_prompt.py` and find `_build_doc_structure_section()`. Verify it accepts `locked_sections` and `unknown_sections` parameters and appends `[RESOLVED: {field}]` or `[NEEDS MAPPING]` annotations inline in the paragraph listing.
**Expected:** Function signature includes locked/unknown params, builds lookup maps, and annotates each paragraph line conditionally.
**Result:**

## P03-T2: Old KB Context Block and Token Budget Removed

**Plan:** 05.6-03 -- Python Annotated Prompt with Inline Resolved/Needs-Mapping Annotations
**Scenario:** Run `grep -c "_build_kb_context_block\|_enforce_token_budget\|_rebuild_kb_block_with_limit\|_estimate_tokens" sanitization-service/app/services/analysis_prompt.py` and verify all four functions are gone.
**Expected:** Count is 0 for all four function names.
**Result:**

## P04-T1: Delete-and-Recreate Replaces Decay

**Plan:** 05.6-04 -- Persistence Overhaul: Delete-and-Recreate Corrections & Auto-Prune
**Scenario:** Open `backend/src/services/templateMapping.ts` and verify: (1) UpsertMode only has 'confirm' and 'create' (no 'correct'), (2) computeConfidence has no 0.7x branch, (3) `deleteAndRecreatMapping()` exists with a `prisma.$transaction` wrapper.
**Expected:** 'correct' mode is gone, no decay logic, delete+create function uses an atomic transaction.
**Result:**

## P04-T2: persistMappingsToKB Uses Source-Aware Reconciliation

**Plan:** 05.6-04 -- Persistence Overhaul: Delete-and-Recreate Corrections & Auto-Prune
**Scenario:** Open `backend/src/services/templateAdapter.ts`, find `persistMappingsToKB()`. Verify it checks `entry.source === 'kb'` to classify confirmations, uses `deleteAndRecreatMapping()` for corrections (different gwField), and calls `pruneDeadEntries()` at the end.
**Expected:** Three-way logic (confirm/correct/create) with source awareness, and auto-prune call at end of function.
**Result:**

## P05-T1: KB Summary Banner Renders Above Mapping Table

**Plan:** 05.6-05 -- Frontend KB Badge, Summary Banner & Muted Row Styling
**Scenario:** Open `frontend/src/features/adapter/components/StepVerify.tsx` and verify a banner div renders when `kbLockedCount > 0`, showing "X sections resolved from Knowledge Base, Y analysed by LLM" with a Brain icon.
**Expected:** Conditional render block with kbLockedCount guard, blue styling, Brain icon import, and count interpolation.
**Result:**

## P05-T2: KB Badge and Muted Row Styling on Mapping Table

**Plan:** 05.6-05 -- Frontend KB Badge, Summary Banner & Muted Row Styling
**Scenario:** Open `frontend/src/features/adapter/components/MappingTable.tsx` and verify: (1) rows with `source === 'kb'` show a slate-colored "KB" badge, (2) KB rows have `bg-muted/20 opacity-75` styling, (3) no event handlers (onRowClick, handleDeleteRow, handleStartEdit) were modified.
**Expected:** KB badge in a flex container next to marker badge, muted row CN conditional, all handlers untouched.
**Result:**
