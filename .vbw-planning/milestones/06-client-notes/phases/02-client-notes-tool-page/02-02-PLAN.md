---
phase: 2
plan: "02"
title: Client-Notes Frontend Data Layer (api + hooks)
type: execute
wave: 1
depends_on: []
cross_phase_deps: ["01"]
autonomous: true
effort_override: balanced
skills_used: []
files_modified:
  - frontend/src/features/schedule/api.ts
  - frontend/src/features/schedule/hooks.ts
  - frontend/src/features/schedule/__tests__/clientNotesApi.test.ts
forbidden_commands: []
must_haves:
  truths:
    - "scheduleApi.getClientNotes(id) calls GET /api/schedule/clients/{id}/notes and is typed to the UNWRAPPED Phase-01 shape { notes: string; notesUpdatedAt: string | null; notesUpdatedBy: string | null } — it returns that object directly, no .client unwrap."
    - "scheduleApi.updateClientNotes(id, notes) calls PUT /api/schedule/clients/{id}/notes with body { notes } and is typed to the WRAPPED Phase-01 shape { client: { notes; notesUpdatedAt; notesUpdatedBy } }; the GET/PUT response-shape asymmetry is a Phase-01 backend fact and is handled here, NOT fixed in the backend."
    - "useClientNotes(id) is a useQuery keyed ['schedule', 'client-notes', id] with enabled: !!id (does not fire for a null/empty id, so it only fetches when a client is selected)."
    - "useUpdateClientNotes() is a useMutation whose mutationFn calls scheduleApi.updateClientNotes(id, notes), returns the unwrapped notes object (reads response.client), and on success invalidates ['schedule', 'client-notes', id] so the modal shows refreshed attribution with no page reload."
    - "useUpdateClientNotes wires onError to handleMutationError(error, 'Failed to save client notes'), consistent with every other mutation in hooks.ts; this does not change the unwrap/invalidate contract (as-built, DEV-01 resolved by plan amendment in R01)."
    - "No backend/** file is modified and no existing schedule api/hook (getClients/useClients, assignments, absences, etc.) is altered — only additive exports."
  artifacts:
    - path: "frontend/src/features/schedule/api.ts"
      provides: "getClientNotes + updateClientNotes API functions handling the GET-unwrapped / PUT-wrapped asymmetry"
      contains: "getClientNotes"
    - path: "frontend/src/features/schedule/hooks.ts"
      provides: "useClientNotes query + useUpdateClientNotes mutation with client-notes cache invalidation"
      contains: "useUpdateClientNotes"
    - path: "frontend/src/features/schedule/__tests__/clientNotesApi.test.ts"
      provides: "fetch-mocked coverage that GET returns unwrapped notes and PUT unwraps { client }"
      contains: "client-notes"
  key_links:
    - from: "frontend/src/features/schedule/hooks.ts"
      to: "frontend/src/features/schedule/api.ts"
      via: "useClientNotes/useUpdateClientNotes call scheduleApi.getClientNotes / scheduleApi.updateClientNotes"
    - from: "frontend/src/features/schedule/api.ts"
      to: "backend GET+PUT /api/schedule/clients/:id/notes (Phase 01)"
      via: "apiClient calls to the Phase-01 read/write endpoints"
---
<objective>
Add the frontend data layer that the client-notes modal (plan 02-03) consumes: two scheduleApi functions and two TanStack Query hooks over the Phase-01 endpoints. No UI, no component — pure api.ts + hooks.ts additions, independent of the NotesEditor lift (disjoint files), so this runs in parallel in wave 1. Backend is untouched: Phase 01 shipped the endpoints; this plan only consumes them.

Decisions resolved (rationale one-liners):
1. GET/PUT shape asymmetry (research open Q2) — type getClientNotes to the unwrapped { notes, notesUpdatedAt, notesUpdatedBy } and updateClientNotes to the wrapped { client: {...} } exactly as Phase 01 returns them; useUpdateClientNotes reads response.client so its consumers see one clean shape. Rationale: Phase 02 has no mandate to touch Phase 01's backend; handle both shapes as-is.
2. Home for the new functions (research open Q6) — add to features/schedule/{api,hooks}.ts alongside the existing client CRUD, NOT a new features/projects/ module. Rationale: the notes live on the Client entity, which already lives in the schedule feature; and the projects-by-client fetch is descoped (name + colour only), so no projects module is warranted.
</objective>
<context>
@.vbw-planning/phases/02-client-notes-tool-page/02-RESEARCH.md
@.vbw-planning/phases/02-client-notes-tool-page/.context-lead.md
Anchor code (validated current, 2026-07-10):
- frontend/src/features/schedule/api.ts:189-208 (existing getClients/createClient/updateClient/deleteClient — the additive extension point; scheduleApi object shape)
- frontend/src/features/schedule/hooks.ts:280-285 (useClients) and :27-100 (invalidateQueries + queryClient pattern to mirror)
- frontend/src/lib/api.ts:51 (apiClient<T>(endpoint, options) — GET default, PUT via { method:'PUT', body: JSON.stringify(...) }; CSRF handled internally)
- Phase 01 backend (do NOT modify): GET /api/schedule/clients/:id/notes returns { notes, notesUpdatedAt, notesUpdatedBy } unwrapped (requireAuth); PUT returns { client: { ... } } wrapped (requireRole('PM')).
Descope note: the modal shows name + colour only — do NOT add any projects/search fetch, hook, or features/projects/ directory (roadmap amended 2026-07-10; research open Q3/Q6 mooted).
</context>
<tasks>
<task type="auto">
  <name>Add getClientNotes + updateClientNotes to scheduleApi</name>
  <files>
    frontend/src/features/schedule/api.ts
  </files>
  <action>
In the scheduleApi object (next to the existing client CRUD, ~api.ts:189-208), add two functions that handle the Phase-01 response-shape asymmetry:
  async getClientNotes(id: string) {
    return apiClient<{ notes: string; notesUpdatedAt: string | null; notesUpdatedBy: string | null }>(
      `/api/schedule/clients/${id}/notes`,
    )
  },
  async updateClientNotes(id: string, notes: string) {
    return apiClient<{ client: { notes: string; notesUpdatedAt: string | null; notesUpdatedBy: string | null } }>(
      `/api/schedule/clients/${id}/notes`,
      { method: 'PUT', body: JSON.stringify({ notes }) },
    )
  },
Add a brief comment on updateClientNotes noting the wrapped { client } shape is intentional (Phase-01 backend fact; not a bug to fix here). Do not alter any existing function.
  </action>
  <verify>
- `grep -n "getClientNotes\|updateClientNotes" frontend/src/features/schedule/api.ts` shows both new functions.
- The GET type is unwrapped ({ notes,... }) and the PUT type is wrapped ({ client: {...} }).
- No existing scheduleApi function was modified (`git diff` shows only additions in the client section).
- LSP/type check clean on api.ts.
  </verify>
  <done>
scheduleApi has getClientNotes (unwrapped) + updateClientNotes (wrapped) over the Phase-01 endpoints. One commit: `feat(schedule): add client-notes read/write api functions`.
  </done>
</task>
<task type="auto">
  <name>Add useClientNotes + useUpdateClientNotes hooks</name>
  <files>
    frontend/src/features/schedule/hooks.ts
  </files>
  <action>
Add two hooks mirroring the existing useQuery/useMutation + queryClient.invalidateQueries conventions in this file:
  export function useClientNotes(id: string | null) {
    return useQuery({
      queryKey: ['schedule', 'client-notes', id],
      queryFn: () => scheduleApi.getClientNotes(id as string),
      enabled: !!id,               // only fetch once a client is selected
    })
  }
  export function useUpdateClientNotes() {
    const queryClient = useQueryClient()
    return useMutation({
      mutationFn: ({ id, notes }: { id: string; notes: string }) =>
        scheduleApi.updateClientNotes(id, notes).then((r) => r.client),  // unwrap the { client } envelope
      onSuccess: (_data, { id }) => {
        queryClient.invalidateQueries({ queryKey: ['schedule', 'client-notes', id] })
      },
      onError: (error: Error) => handleMutationError(error, 'Failed to save client notes'),  // file-wide error convention
    })
  }
Ensure useQuery/useMutation/useQueryClient are already imported at the top (they are — used by existing hooks). Do not alter any existing hook.
As-built note (DEV-01, R01): the onError handler above mirrors the identical handleMutationError pattern used by all 18 other mutations in hooks.ts; it is required for convention consistency and does not alter the mutationFn/unwrap('r.client')/invalidate(['schedule','client-notes',id]) contract.
  </action>
  <verify>
- `grep -n "useClientNotes\|useUpdateClientNotes" frontend/src/features/schedule/hooks.ts` shows both hooks.
- useClientNotes uses queryKey ['schedule','client-notes',id] with enabled: !!id; useUpdateClientNotes unwraps r.client and invalidates the same key on success.
- No existing hook modified.
- LSP/type check clean on hooks.ts.
  </verify>
  <done>
useClientNotes (guarded query) + useUpdateClientNotes (unwrapping mutation that invalidates the client-notes key) exist. One commit: `feat(schedule): add client-notes query and mutation hooks`.
  </done>
</task>
<task type="auto">
  <name>Fetch-mocked test for the api-layer GET/PUT shape handling</name>
  <files>
    frontend/src/features/schedule/__tests__/clientNotesApi.test.ts
  </files>
  <action>
Create frontend/src/features/schedule/__tests__/clientNotesApi.test.ts that stubs global.fetch (vi.spyOn(globalThis,'fetch')) and exercises the two api functions directly (no React, no hooks — apiClient uses fetch under the hood). Cases:
  1. getClientNotes('c1') hits `/api/schedule/clients/c1/notes` with no method (GET) and returns the unwrapped { notes, notesUpdatedAt, notesUpdatedBy } object as-is.
  2. updateClientNotes('c1','hello') issues a PUT to the same URL with a JSON body containing { notes: 'hello' } and returns the wrapped { client: {...} } shape (assert the caller can read .client).
Mock fetch to resolve a Response-like object ({ ok: true, status: 200, json: async () => (...) , headers }) sufficient for apiClient. If CSRF token acquisition in apiClient complicates the PUT path, stub ensureCsrfToken/getCsrfToken from '@/lib/api' or provide a token via the fetch mock as the existing api tests do. Keep it minimal and green.
  </action>
  <verify>
- `cd frontend && npx vitest run src/features/schedule/__tests__/clientNotesApi.test.ts` passes.
- The test asserts the GET-unwrapped and PUT-wrapped shapes and the PUT request URL/method/body.
  </verify>
  <done>
The GET-unwrapped / PUT-wrapped asymmetry is pinned by a test so a future refactor cannot silently break the modal. One commit: `test(schedule): cover client-notes api GET/PUT response shapes`.
  </done>
</task>
</tasks>
<verification>
1. getClientNotes typed/returns unwrapped { notes, notesUpdatedAt, notesUpdatedBy }; updateClientNotes typed to wrapped { client: {...} }.
2. useClientNotes keyed ['schedule','client-notes',id], enabled only when id is set; useUpdateClientNotes unwraps .client and invalidates that key on success.
3. No backend file changed; no existing schedule api/hook altered — additive only.
4. Typecheck clean; the api-shape test is green.
</verification>
<success_criteria>
- A frontend read path returns a client's current notes + attribution (unwrapped) for the modal to display.
- A frontend write path persists notes and, on success, invalidates the client-notes cache so refreshed attribution appears with no page reload.
- The Phase-01 GET/PUT response-shape asymmetry is handled entirely in the frontend api layer; the backend is untouched.
</success_criteria>
<output>
02-02-SUMMARY.md
</output>

<!-- UAT-staleness re-QA re-affirmation (QA remediation round R01, 2026-07-14) -->
<!--
DEV-01 (plan-amendment) re-affirmed against current product HEAD 9f3cac3 after Phase 02's
prior QA was re-run for verified_at_commit staleness (8 product-code commits had landed since
the original verification at 2ba8340, touching client-notes files: NotesEditor.tsx,
CardDetailModal.tsx, board types/service).

Re-verified at HEAD 9f3cac3:
- The as-built `onError: (error: Error) => handleMutationError(error, 'Failed to save client notes'),`
  line in Task 2's useUpdateClientNotes snippet above still matches frontend/src/features/schedule/hooks.ts
  (:335-345) byte-for-byte; the handler mirrors the file-wide handleMutationError convention (18 sibling
  mutations) and does not change the mutationFn/unwrap('r.client')/invalidate(['schedule','client-notes',id]) contract.
- No frontend/ or backend/ product code was changed by this re-affirmation. Full frontend suite: 87/87 green.
- The two pre-existing Sidebar.tsx ESLint findings remain accepted-process-exception (see R01-DISPOSITION via
  remediation/qa/round-01/R01-PLAN.md; DEVN-05 precedent). They reproduce at HEAD on lines untouched by Phase 02.

DEV-01 stays resolved-by-amendment. Do NOT strip the onError handler.
-->

