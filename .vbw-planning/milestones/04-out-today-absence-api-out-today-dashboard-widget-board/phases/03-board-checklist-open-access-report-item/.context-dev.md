## Phase 03 Context

### Milestone Scope Context

Gathered: 2026-06-29
Calibration: builder

## Scope Boundary

Two threads of work on the existing platform:
1. **Out-Today dashboard widget** (Phases 1–2): a read-only "Out Today" view on the Dashboard listing users absent on the current day, backed by existing `Absence` scheduling data. Original request: "We need some kind of widget in the dashboard listing the users that are out on that day."
2. **Board checklist adjustments** (Phase 3, added mid-milestone): open checklist check/edit access to all authenticated users, and add a standard `Report is on client's share` checklist item to all project cards. Request: "everyone can review the checklist of all projects" + "add a new line to the checklist with the text 'Report is on client's share'."

## Decomposition Decisions

### Phase Count & Grouping
3 phases:
- Phase 1 (backend): a query/endpoint that returns who is out on a given date, reusing `Absence` + `absenceService.ts`.
- Phase 2 (frontend): the Dashboard widget that consumes it.
- Phase 3 (backend-only): board checklist open-access authz change + default/backfilled `Report is on client's share` item. Independent of Phases 1–2 (different domain: Board/Kanban), added after they were built+verified.
Phases 1–2 split along the codebase's routes → services → frontend layering, keeping each independently plannable/testable. Phase 3 is grouped as a single backend phase because both of its changes are small, surgical, and touch the same board/checklist surface.

### Phase Ordering
Phase 1 before Phase 2 (the widget consumes the Phase 1 API — contract first). Phase 3 last, appended after 1–2 shipped; no dependency on them.

### Scope Coverage
Covers: (1) surfacing existing absence data for "today" as a dashboard widget; (2) opening board checklist check/edit to any authenticated user and adding the report-share checklist item everywhere.
Excluded / deferred: creating/editing absences, absence approval workflows, date-range/"out this week" absence views, notifications; opening the board `notes` field to non-owners (checklist only); changing the ADMIN-only archive and PM/ADMIN-only stage-lock restrictions.

## Requirement Mapping

| Phase | Requirements |
|-------|--------------|
| 1 — Out-Today Absence API | Scheduling/planner (absence data), RBAC (authenticated read consistent with `/api/schedule`) |
| 2 — Out-Today Dashboard Widget | Dashboard (widget surface), Scheduling/planner (absence display) |
| 3 — Board Checklist Open Access & Report-Share Item | Board (Kanban checklist), RBAC (authenticated checklist write; ADMIN/PM archive + stage-lock kept restricted) |

## Key Decisions

- Reuse the existing `Absence` Prisma model and `absenceService.ts` rather than introducing a new absence data model.
- Surface "out today" through the existing schedule/absence route surface, with authz consistent with current `/api/schedule` access (server authoritative).
- Build the widget inside the feature-sliced `features/dashboard` structure using shared shadcn UI primitives and the established React Query data pattern.
- Phase 3: gate checklist writes per-field — a `checklist`-only PATCH body from any authenticated user skips the assignment-ownership check; any other field keeps the existing ownership/PM/ADMIN gating. Keep `BoardCard.checklist` as free-form JSON-in-TEXT (no schema change). Seed the new default item in `DEFAULT_CHECKLIST` and backfill existing cards with a manual idempotent `npx tsx` script (matches repo one-off-script convention).

## Deferred Ideas

- "Out this week" / date-range absence views (a Phase 03 for this was scoped and then aborted by the user — the projects dashboard uses two always-visible Current/Next columns, not a toggle; revisit if wanted).
- Absence creation/editing from the dashboard widget.
- Notifications when someone is out.
- Opening the board `notes` field to non-owners (Phase 3 opened checklist only).


### Goal
Two independent board-checklist changes to the Kanban board.

### Conventions
- [null] Backend files use camelCase, frontend components use PascalCase, Python uses snake_case
- [null] Use @/ import alias for src directory in both backend and frontend
- [null] Feature modules follow features/{domain}/api.ts + hooks.ts pattern
- [null] Routes delegate to service layer; no business logic in route handlers
- [null] Zod validation at all boundaries (env config, route input, form schemas)
- [null] TanStack Query for all server state management; no manual fetch calls
- [null] Pydantic models for all FastAPI request/response schemas
- [null] Commit format: {type}({scope}): {description}

### Codebase Map Available
Codebase mapping exists in `.vbw-planning/codebase/`. Key files:
- `ARCHITECTURE.md`
- `CONCERNS.md`
- `PATTERNS.md`
- `DEPENDENCIES.md`
- `STRUCTURE.md`
- `CONVENTIONS.md`
- `TESTING.md`
- `STACK.md`

Read CONVENTIONS.md, PATTERNS.md, STRUCTURE.md, and DEPENDENCIES.md first to bootstrap codebase understanding.

### Changed Files (Delta)
- `.vbw-planning/.active-agents/23b07cf6-3d47-4199-aa04-965190799802/active-agent`
- `.vbw-planning/.active-agents/23b07cf6-3d47-4199-aa04-965190799802/active-agent-count`
- `.vbw-planning/.active-agents/23b07cf6-3d47-4199-aa04-965190799802/active-agent-role-pids`
- `.vbw-planning/.active-agents/23b07cf6-3d47-4199-aa04-965190799802/active-agent-roles`
- `.vbw-planning/.active-agents/7031869b-1834-4e32-83f4-8b736d1be2ad/active-agent`
- `.vbw-planning/.active-agents/7031869b-1834-4e32-83f4-8b736d1be2ad/active-agent-count`
- `.vbw-planning/.active-agents/7031869b-1834-4e32-83f4-8b736d1be2ad/active-agent-role-pids`
- `.vbw-planning/.active-agents/7031869b-1834-4e32-83f4-8b736d1be2ad/active-agent-roles`
- `.vbw-planning/.agent-pids`
- `.vbw-planning/.cache/context-index.json`
- `.vbw-planning/.context-usage`
- `.vbw-planning/.cost-ledger.json`
- `.vbw-planning/discovery.json`
- `.vbw-planning/.event-log.jsonl`
- `.vbw-planning/.events/event-log.jsonl`
- `.vbw-planning/.hook-errors.log`
- `.vbw-planning/milestones/03-pm-project-delete-split-project-locking/STATE.md`
- `.vbw-planning/.notification-log.jsonl`
- `.vbw-planning/.session-log.jsonl`
- `.vbw-planning/.skill-decisions.log`
- `.vbw-planning/todo-details.json`

### Code Slices

#### `.vbw-planning/.agent-pids` (4 lines)
```
208094
238429
255670
270894
```

#### `.vbw-planning/.cache/context-index.json` (358 lines, first 30 shown)
```
{
  "entries": {
    "e6e12f389208a173": {
      "path": "/home/rm/Documents/Layer8-Management-Platform/.vbw-planning/.cache/context/e6e12f389208a173.md",
      "role": "qa",
      "phase": "22",
      "timestamp": "2026-05-29T17:47:16Z"
    },
    "668e5e41e1e6b114": {
      "path": "/home/rm/Documents/Layer8-Management-Platform/.vbw-planning/.cache/context/668e5e41e1e6b114.md",
      "role": "qa",
      "phase": "23",
      "timestamp": "2026-06-01T12:56:46Z"
    },
    "d08cb2325d8e36e3": {
      "path": "/home/rm/Documents/Layer8-Management-Platform/.vbw-planning/.cache/context/d08cb2325d8e36e3.md",
      "role": "qa",
      "phase": "24",
      "timestamp": "2026-06-03T09:18:11Z"
    },
    "ae4b3bbb6ba85420": {
      "path": "/home/rm/Documents/Layer8-Management-Platform/.vbw-planning/.cache/context/ae4b3bbb6ba85420.md",
      "role": "scout",
      "phase": "01",
      "timestamp": "2026-06-03T16:11:23Z"
    },
    "af767e94ffd1a4af": {
      "path": "/home/rm/Documents/Layer8-Management-Platform/.vbw-planning/.cache/context/af767e94ffd1a4af.md",
      "role": "lead",
      "phase": "01",
```

#### `.vbw-planning/.context-usage` (1 lines)
```
unknown|67|967000
```

#### `.vbw-planning/.cost-ledger.json` (3 lines)
```
{
  "other": 229
}
```

#### `.vbw-planning/discovery.json` (1452 lines, first 30 shown)
```
{
  "answered": [
    {
      "category": "workflow",
      "phase": 5,
      "question": "Input type (blank DOCX vs existing GW)",
      "answer": "Blank client DOCX",
      "date": "2026-02-13"
    },
    {
      "category": "ux",
      "phase": 5,
      "question": "UI layout (wizard vs single page)",
      "answer": "Step-by-step wizard",
      "date": "2026-02-13"
    },
    {
      "category": "workflow",
      "phase": 5,
      "question": "User-provided metadata",
      "answer": "Template type + language selection",
      "date": "2026-02-13"
    },
    {
      "category": "workflow",
      "phase": 5,
      "question": "Final output format",
      "answer": "Download DOCX only",
      "date": "2026-02-13"
    },
```

#### `.vbw-planning/.event-log.jsonl` (55 lines, first 30 shown)
```
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T16:12:33Z"}
{"event":"bash_guard_block","command_preview":"grep -r \"autoScroll\" /home/rm/Documents/","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T16:12:37Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T16:12:41Z"}
{"event":"bash_guard_block","command_preview":"grep -n \"resetAutoMove\|reset-auto-move\","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T16:13:36Z"}
{"event":"bash_guard_block","command_preview":"grep -rn \"resetAutoMove\|reset-auto-move","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T16:13:37Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T17:25:31Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T17:25:34Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T17:25:35Z"}
{"event":"bash_guard_block","command_preview":"git log --oneline e821b4f 37fd3d4 968c34","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T17:25:54Z"}
{"event":"bash_guard_block","command_preview":"git show 37fd3d4 --stat 2>/dev/null","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T17:25:57Z"}
{"event":"bash_guard_block","command_preview":"git -C /home/rm/Documents/Layer8-Managem","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T17:26:00Z"}
{"event":"bash_guard_block","command_preview":"grep -rn \"confirmProjectName\|PROJECT_NA","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T17:55:39Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T17:55:40Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T17:55:51Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T19:06:27Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T19:06:31Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T19:06:41Z"}
{"event":"bash_guard_block","command_preview":"grep -n \"include\|exclude\|threads\|pool","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T19:07:14Z"}
{"event":"bash_guard_block","command_preview":"grep -r \"KanbanCard\" /home/rm/Documents/","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T20:02:01Z"}
{"event":"bash_guard_block","command_preview":"grep -r \"KanbanCard\" /home/rm/Documents/","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T20:02:05Z"}
{"event":"bash_guard_block","command_preview":"ls /home/rm/Documents/Layer8-Management-","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T20:02:41Z"}
{"event":"bash_guard_block","command_preview":"ls /home/rm/Documents/Layer8-Management-","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-03T21:46:45Z"}
{"event":"bash_guard_block","command_preview":"grep -rn \"stringToColor\|hashColor\|colo","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-11T09:08:33Z"}
{"event":"bash_guard_block","command_preview":"grep -rn \"getInitials\|toInitials\|initi","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-11T09:08:34Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-11T09:08:41Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-11T09:08:42Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-11T09:08:54Z"}
{"event":"bash_guard_block","command_preview":"find /home/rm/Documents/Layer8-Managemen","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-11T09:08:55Z"}
{"event":"bash_guard_block","command_preview":"grep -n \"firstName\|lastName\|first_name","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-11T09:09:41Z"}
{"event":"bash_guard_block","command_preview":"grep -n \"displayName\|username\|User \" /","pattern_matched":"scout:shell file write/redirection","agent":"scout","timestamp":"2026-06-11T09:09:42Z"}
```

#### `.vbw-planning/.events/event-log.jsonl` (46 lines)
```
{"ts":"2026-06-03T13:26:03Z","event_id":"90012955-a8bf-4ad8-9577-d5c51c297508","correlation_id":"","event":"milestone_shipped","phase":"archive","data":{"slug":"01-project-board","archive_path":"/home/rm/Documents/Layer8-Management-Platform/.vbw-planning/milestones/01-project-board","tag":"milestone/project-board"}}
{"ts":"2026-06-03T16:26:23Z","event_id":"f6730dce-3db6-4892-8e22-f15f0d332502","correlation_id":"c4377621-9cdf-493d-bf9d-34001967627e","event":"phase_start","phase":1}
{"ts":"2026-06-03T17:10:05Z","event_id":"1206368a-ca82-40ce-8adc-51ef6c556d86","correlation_id":"","event":"plan_end","phase":1,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-06-03T18:07:32Z","event_id":"eac6ed5b-49b1-4b40-893c-63ac541bcd86","correlation_id":"8391af41-99a6-4080-ada2-acec4114b2cd","event":"phase_start","phase":2}
{"ts":"2026-06-03T18:08:54Z","event_id":"4159b7f7-56da-431f-9878-8e5657df6aa9","correlation_id":"8391af41-99a6-4080-ada2-acec4114b2cd","event":"plan_start","phase":2,"plan":1}
{"ts":"2026-06-03T18:08:54Z","event_id":"726e9215-68f7-4180-acf1-99a47dc41116","correlation_id":"8391af41-99a6-4080-ada2-acec4114b2cd","event":"agent_spawn","phase":2,"plan":1,"data":{"role":"dev","model":"opus"}}
{"ts":"2026-06-03T18:16:55Z","event_id":"939f4b7d-fd2d-4ad4-bf23-93dc3843a35d","correlation_id":"8391af41-99a6-4080-ada2-acec4114b2cd","event":"plan_end","phase":2,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-06-03T18:25:20Z","event_id":"eedb468f-3898-4b4d-966e-bca631c81cf8","correlation_id":"8391af41-99a6-4080-ada2-acec4114b2cd","event":"gate_passed","phase":2,"plan":1,"data":{"gate":"verification_threshold","task":"3","evidence":"phase dir not found"}}
{"ts":"2026-06-03T19:01:46Z","event_id":"ce6456ac-2dc7-4ff2-95b4-9c0418587b72","correlation_id":"8391af41-99a6-4080-ada2-acec4114b2cd","event":"phase_end","phase":2,"data":{"status":"complete"}}
{"ts":"2026-06-03T19:13:42Z","event_id":"547ac97a-4bf9-40b3-803e-58a37d549193","correlation_id":"6a141731-3584-4f08-8ab6-82ef500d7932","event":"phase_start","phase":3}
{"ts":"2026-06-03T19:13:55Z","event_id":"31f51071-b1e5-406d-904f-857cd7a9b992","correlation_id":"6a141731-3584-4f08-8ab6-82ef500d7932","event":"plan_start","phase":3,"plan":1}
{"ts":"2026-06-03T19:13:55Z","event_id":"25c58203-2d21-40ee-b1f4-de6398323355","correlation_id":"6a141731-3584-4f08-8ab6-82ef500d7932","event":"agent_spawn","phase":3,"plan":1,"data":{"role":"dev","model":"opus"}}
{"ts":"2026-06-03T19:19:54Z","event_id":"aad2491e-fa35-4bc6-8523-1b53b8965041","correlation_id":"6a141731-3584-4f08-8ab6-82ef500d7932","event":"plan_end","phase":3,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-06-03T19:31:04Z","event_id":"d6a19484-2ca0-4209-9617-019861360dfc","correlation_id":"6a141731-3584-4f08-8ab6-82ef500d7932","event":"gate_passed","phase":3,"plan":1,"data":{"gate":"verification_threshold","task":"2","evidence":"phase dir not found"}}
{"ts":"2026-06-03T19:38:12Z","event_id":"c26f85c1-697b-4efb-b116-84437e09c790","correlation_id":"6a141731-3584-4f08-8ab6-82ef500d7932","event":"phase_end","phase":3,"data":{"status":"complete"}}
{"ts":"2026-06-03T20:10:27Z","event_id":"e6dc717c-3e7f-407a-a5eb-f290f4f44045","correlation_id":"38727152-ec61-4303-9d46-12298b407d51","event":"phase_start","phase":4}
{"ts":"2026-06-03T20:10:36Z","event_id":"c7f9ba43-9f04-4ee2-9471-596c78ec099f","correlation_id":"38727152-ec61-4303-9d46-12298b407d51","event":"plan_start","phase":4,"plan":1}
{"ts":"2026-06-03T20:16:58Z","event_id":"2d41b27a-32c0-444d-bd4e-792fea043339","correlation_id":"38727152-ec61-4303-9d46-12298b407d51","event":"plan_end","phase":4,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-06-03T21:53:21Z","event_id":"4ecdae4a-86dc-4419-a7ea-1a60f1f920da","correlation_id":"2801a720-ac0f-44fd-b4ba-9695a726eb0c","event":"phase_start","phase":5}
{"ts":"2026-06-03T21:53:39Z","event_id":"bc3dedbe-67cf-48d7-8241-8279108751c6","correlation_id":"2801a720-ac0f-44fd-b4ba-9695a726eb0c","event":"plan_start","phase":5,"plan":1}
{"ts":"2026-06-03T21:58:11Z","event_id":"29086eee-99ca-4ee2-a590-52acdfa2e9e1","correlation_id":"2801a720-ac0f-44fd-b4ba-9695a726eb0c","event":"plan_end","phase":5,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-06-03T21:58:11Z","event_id":"28d39183-e646-4795-8650-c278cbc464db","correlation_id":"2801a720-ac0f-44fd-b4ba-9695a726eb0c","event":"plan_start","phase":5,"plan":2}
{"ts":"2026-06-03T22:00:47Z","event_id":"d7bd1ac2-d4c4-4d4b-910d-0390307e4548","correlation_id":"2801a720-ac0f-44fd-b4ba-9695a726eb0c","event":"plan_end","phase":5,"plan":2,"data":{"status":"complete"}}
{"ts":"2026-06-04T10:39:17Z","event_id":"a697cca4-0df4-44ee-ab60-0d1b066cc039","correlation_id":"6ef0f8c6-48b2-49e4-8bde-67cfb9018b92","event":"phase_start","phase":6}
{"ts":"2026-06-04T10:39:18Z","event_id":"e1d684aa-e20e-4423-8b06-136e62c1f12e","correlation_id":"6ef0f8c6-48b2-49e4-8bde-67cfb9018b92","event":"plan_start","phase":6,"plan":1}
{"ts":"2026-06-04T12:18:45Z","event_id":"95e224d0-b416-4150-80cb-7c89cd121533","correlation_id":"6ef0f8c6-48b2-49e4-8bde-67cfb9018b92","event":"plan_end","phase":6,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-06-11T09:32:52Z","event_id":"5eced0cb-9946-42fd-bbc3-6473b17850e5","correlation_id":"24e2a37e-c172-400a-bcd2-f29712b4c35e","event":"phase_start","phase":7}
{"ts":"2026-06-11T09:39:08Z","event_id":"a28e1ce0-5844-4d39-bb4c-0faf682db6fe","correlation_id":"24e2a37e-c172-400a-bcd2-f29712b4c35e","event":"plan_end","phase":7,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-06-11T15:34:09Z","event_id":"24d06915-2a68-4a75-9488-f864e62ede06","correlation_id":"1d46057e-687b-4537-8c06-ed22f1cc6e7d","event":"phase_start","phase":8}
{"ts":"2026-06-11T15:46:17Z","event_id":"7d0c5d1e-ae7e-4462-82a3-5a25fcb703f3","correlation_id":"1d46057e-687b-4537-8c06-ed22f1cc6e7d","event":"plan_end","phase":8,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-06-15T23:34:28Z","event_id":"bec22418-c835-4c4a-a7ab-d695658e605e","correlation_id":"","event":"milestone_shipped","phase":"archive","data":{"slug":"02-board-refinements","archive_path":"/home/rm/Documents/Layer8-Management-Platform/.vbw-planning/milestones/02-board-refinements","tag":"milestone/02-board-refinements"}}
{"ts":"2026-06-22T22:52:15Z","event_id":"f7d05df9-347a-4646-936f-89290039bf76","correlation_id":"d6615659-f5f6-4288-8ba9-d00046faf5ee","event":"phase_start","phase":1}
{"ts":"2026-06-22T22:52:48Z","event_id":"c4449d46-2448-4bd0-8ded-2fa91d6636f4","correlation_id":"d6615659-f5f6-4288-8ba9-d00046faf5ee","event":"plan_start","phase":1,"plan":1}
{"ts":"2026-06-22T22:52:48Z","event_id":"400bfc8b-df98-41dd-96b1-44ce13bc5a61","correlation_id":"d6615659-f5f6-4288-8ba9-d00046faf5ee","event":"plan_start","phase":1,"plan":2}
{"ts":"2026-06-22T22:59:55Z","event_id":"d8d29823-011e-47d1-8be3-d40fb1e352e2","correlation_id":"d6615659-f5f6-4288-8ba9-d00046faf5ee","event":"plan_end","phase":1,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-06-22T23:08:34Z","event_id":"a23d37f0-a6fb-4b25-8b4a-f69ab552cd4e","correlation_id":"d6615659-f5f6-4288-8ba9-d00046faf5ee","event":"plan_end","phase":1,"plan":2,"data":{"status":"complete"}}
{"ts":"2026-06-22T23:14:29Z","event_id":"14dd50e7-fa2b-49f7-b9ff-a46af248d017","correlation_id":"d6615659-f5f6-4288-8ba9-d00046faf5ee","event":"gate_failed","phase":1,"plan":1,"data":{"gate":"verification_threshold","task":"0","evidence":"verification invalid"}}
{"ts":"2026-06-23T18:12:18Z","event_id":"ce863ccd-b598-481f-8001-e75c23113044","correlation_id":"d6615659-f5f6-4288-8ba9-d00046faf5ee","event":"phase_end","phase":1,"data":{"plans_completed":"2","total_tasks":"9"}}
{"ts":"2026-06-29T20:05:52Z","event_id":"79360153-e981-4e1f-8857-a41a1c462ab8","correlation_id":"","event":"milestone_shipped","phase":"archive","data":{"slug":"03-pm-project-delete-split-project-locking","archive_path":"/home/rm/Documents/Layer8-Management-Platform/.vbw-planning/milestones/03-pm-project-delete-split-project-locking","tag":"milestone/03-pm-project-delete-split-project-locking"}}
{"ts":"2026-06-29T20:53:36Z","event_id":"4fe8d968-2bb8-4c7e-8066-cbb46cc3b583","correlation_id":"fdcf7196-9b7a-49a0-94bb-2a90a4771349","event":"phase_start","phase":1}
{"ts":"2026-06-29T20:54:01Z","event_id":"643a08d5-f48d-481b-93b3-c7f68efba4c9","correlation_id":"fdcf7196-9b7a-49a0-94bb-2a90a4771349","event":"plan_start","phase":1,"plan":1}
{"ts":"2026-06-29T21:10:15Z","event_id":"5f0469da-bb8c-4ca4-9831-82ba9b51ff9d","correlation_id":"fdcf7196-9b7a-49a0-94bb-2a90a4771349","event":"plan_end","phase":1,"plan":1,"data":{"status":"complete"}}
{"ts":"2026-07-02T15:19:05Z","event_id":"3e838f8e-be62-4011-bf99-a6b0f454768f","correlation_id":"fdcf7196-9b7a-49a0-94bb-2a90a4771349","event":"phase_end","phase":1,"data":{"plans_completed":"1","total_tasks":"3"}}
{"ts":"2026-07-02T15:32:32Z","event_id":"d876c0e2-1297-45d6-806c-abf4b65e0a1e","correlation_id":"436dee52-3ac8-4fa9-be37-5dbf9365b344","event":"phase_start","phase":2}
{"ts":"2026-07-02T15:32:32Z","event_id":"ce1634f7-8c26-4fec-81f2-f12ed43f4a0f","correlation_id":"436dee52-3ac8-4fa9-be37-5dbf9365b344","event":"plan_start","phase":2,"plan":1}
{"ts":"2026-07-02T15:57:31Z","event_id":"55c0ea12-9e14-415d-a78d-1e4907ad92b1","correlation_id":"436dee52-3ac8-4fa9-be37-5dbf9365b344","event":"plan_end","phase":2,"plan":1,"data":{"status":"complete"}}
```

#### `.vbw-planning/.hook-errors.log` (47 lines)
```
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 8 has no matching 8-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 9 has no matching 9-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 10 has no matching 10-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 11 has no matching 11-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 12 has no matching 12-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 13 has no matching 13-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 14 has no matching 14-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 15 has no matching 15-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 16 has no matching 16-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 17 has no matching 17-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 18 has no matching 18-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 19 has no matching 19-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 20 has no matching 20-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:31:57Z] state-numbering-warning: ROADMAP phase 21 has no matching 21-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 1 has no matching 1-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 2 has no matching 2-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 3 has no matching 3-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 4 has no matching 4-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 5 has no matching 5-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 6 has no matching 6-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 7 has no matching 7-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 8 has no matching 8-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 9 has no matching 9-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 10 has no matching 10-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 11 has no matching 11-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 12 has no matching 12-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:07Z] state-numbering-warning: ROADMAP phase 13 has no matching 13-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:08Z] state-numbering-warning: ROADMAP phase 14 has no matching 14-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:08Z] state-numbering-warning: ROADMAP phase 15 has no matching 15-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:08Z] state-numbering-warning: ROADMAP phase 16 has no matching 16-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:08Z] state-numbering-warning: ROADMAP phase 17 has no matching 17-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:08Z] state-numbering-warning: ROADMAP phase 18 has no matching 18-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:08Z] state-numbering-warning: ROADMAP phase 19 has no matching 19-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:08Z] state-numbering-warning: ROADMAP phase 20 has no matching 20-* phase directory; using prefix numbering and preserving missing phase slot
[2026-06-03T10:32:08Z] state-numbering-warning: ROADMAP phase 21 has no matching 21-* phase directory; using prefix numbering and preserving missing phase slot
2026-06-03T15:35:00Z map-staleness.sh exit=1
2026-06-03T18:01:08Z map-staleness.sh exit=1
2026-06-05T08:44:49Z map-staleness.sh exit=1
2026-06-12T15:53:05Z map-staleness.sh exit=1
2026-06-12T16:43:19Z map-staleness.sh exit=1
2026-06-15T22:58:20Z map-staleness.sh exit=1
2026-06-22T22:05:30Z map-staleness.sh exit=1
2026-06-22T22:51:02Z map-staleness.sh exit=1
2026-06-23T18:37:25Z map-staleness.sh exit=1
2026-06-29T19:59:45Z map-staleness.sh exit=1
2026-06-29T20:43:56Z map-staleness.sh exit=1
2026-07-02T16:01:01Z map-staleness.sh exit=1
```

#### `.vbw-planning/milestones/03-pm-project-delete-split-project-locking/STATE.md` (64 lines, first 30 shown)
```
# State

**Project:** Template AI Engine (Layer8)
**Milestone:** PM Project Controls

## Current Phase
Phase: 1 of 1 (Pm Project Delete And Lock)
Plans: 2/2
Progress: 100%
Status: complete

## Phase Status
- **Phase 1 (Pm Project Delete And Lock):** Complete

## Key Decisions
| Decision | Date | Rationale |
|----------|------|-----------|
| CLIProxyAPI as primary LLM provider (OpenAI SDK format) | | |
| Anthropic API as fallback (only if CLIProxy unavailable) | | |
| Per-feature model config: Sonnet 4.5 for templates, Opus 4.6 for reports | | |
| Manual retry only (no auto-retry to avoid burning credits) | | |
| Full sanitized prompts stored in audit log for GDPR compliance | | |
| python-docx in sanitization service for DOCX operations | | |
| Gotenberg Docker container for PDF generation (dev + prod) | | |
| Ghostwriter always reachable (no offline fallback) | | |
| react-pdf for PDF preview, strict upload validation | | |
| docxtpl for Jinja2 template rendering (native GW template syntax support) | | |

## Todos
- [SIDE-FINDING] ArchiveCardDialog has empty-projectName UX edge case — when a card has no linked schedule assignment, the typed-confirmation target is empty string and the user has no visible name to type. Recommend disabling Archive when no project, or using a literal "DELETE" sentinel. (Surfaced in Phase 23 UAT R01, P05-T2; not blocking — typical cards have project names. See remediation/uat/round-01/R01-UAT.md.)
```

#### `.vbw-planning/.notification-log.jsonl` (4368 lines, first 30 shown)
```
{
  "timestamp": "2026-02-12T18:21:21Z",
  "type": "permission_prompt",
  "title": "",
  "message": "Claude Code needs your attention"
}
{
  "timestamp": "2026-02-12T18:29:54Z",
  "type": "permission_prompt",
  "title": "",
  "message": "Claude Code needs your attention"
}
{
  "timestamp": "2026-02-12T18:43:36Z",
  "type": "idle_prompt",
  "title": "",
  "message": "Claude is waiting for your input"
}
{
  "timestamp": "2026-02-12T18:46:56Z",
  "type": "idle_prompt",
  "title": "",
  "message": "Claude is waiting for your input"
}
{
  "timestamp": "2026-02-12T20:10:27Z",
  "type": "idle_prompt",
  "title": "",
  "message": "Claude is waiting for your input"
}
```

#### `.vbw-planning/.session-log.jsonl` (11317 lines, first 30 shown)
```
{
  "timestamp": "2026-02-12T18:24:29Z",
  "duration_ms": 0,
  "cost_usd": 0,
  "tokens_in": 0,
  "tokens_out": 0,
  "model": "unknown",
  "branch": "master"
}
{
  "timestamp": "2026-02-12T18:24:29Z",
  "type": "cost_summary",
  "costs": {
    "other": 587
  }
}
{
  "timestamp": "2026-02-12T18:24:36Z",
  "duration_ms": 0,
  "cost_usd": 0,
  "tokens_in": 0,
  "tokens_out": 0,
  "model": "unknown",
  "branch": "master"
}
{
  "timestamp": "2026-02-12T18:24:36Z",
  "type": "cost_summary",
  "costs": {
    "other": 9
```

#### `.vbw-planning/.skill-decisions.log` (134 lines, first 30 shown)
```
{"ts":"2026-05-29T17:48:21Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is static contract verification of existing TypeScript/React source against a plan's must_haves..."}
{"ts":"2026-05-29T17:51:18Z","session":"unknown","agent":"vbw:vbw-lead","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is remediation planning for a single React-component sorting FAIL; available skills (interactiv..."}
{"ts":"2026-05-29T17:53:38Z","session":"unknown","agent":"vbw:vbw-dev","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: the task is a single markdown plan-amendment edit to a planning document; no installed skill (intera..."}
{"ts":"2026-05-29T17:55:51Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is static re-verification of a documentation plan-amendment plus the original FAIL; no installe..."}
{"ts":"2026-05-29T18:04:14Z","session":"unknown","agent":"vbw:vbw-lead","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is remediation planning for documentation/bookkeeping reconciliation of markdown artifacts; no ..."}
{"ts":"2026-05-29T18:06:40Z","session":"unknown","agent":"vbw:vbw-dev","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: the task is markdown plan/summary bookkeeping reconciliation; no installed skill applies."}
{"ts":"2026-05-29T18:10:31Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: static re-verification of plan/summary bookkeeping reconciliation plus the original FAIL; no install..."}
{"ts":"2026-06-01T12:58:02Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: phase-23 QA is contract verification of a React/Express feature against plan must-haves; no installe..."}
{"ts":"2026-06-01T13:18:20Z","session":"unknown","agent":"vbw:vbw-lead","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is a TypeScript/Express/Prisma JSDoc correction plus a VBW remediation-plan write; no installed..."}
{"ts":"2026-06-01T13:21:13Z","session":"unknown","agent":"vbw:vbw-dev","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is a small TypeScript JSDoc/comment correction plus additive markdown planning-doc amendments; ..."}
{"ts":"2026-06-01T13:26:05Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: re-verifying a small TypeScript JSDoc correction plus markdown planning-doc amendments; no installed..."}
{"ts":"2026-06-03T09:20:53Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: TypeScript/Express/Prisma/React + Vitest verification of an already-built phase; no installed skill ..."}
{"ts":"2026-06-03T09:28:48Z","session":"unknown","agent":"vbw:vbw-lead","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is a remediation-plan write for a Vitest test-isolation fix; no installed skill matches the pla..."}
{"ts":"2026-06-03T09:31:18Z","session":"unknown","agent":"vbw:vbw-dev","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is a Vitest test-isolation fix in an existing TS/Prisma backend; no matching installed skill is..."}
{"ts":"2026-06-03T09:37:42Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: re-verifying a Vitest test-isolation fix in a TS/Prisma backend; no matching installed skill."}
{"ts":"2026-06-03T16:12:05Z","session":"unknown","agent":"vbw:vbw-scout","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: no installed skill matches this React/TypeScript/@dnd-kit board-UI research task; the only installed..."}
{"ts":"2026-06-03T16:16:25Z","session":"unknown","agent":"vbw:vbw-lead","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: no installed skill matches this React/TypeScript/@dnd-kit board planning task; the only installed sk..."}
{"ts":"2026-06-03T16:26:44Z","session":"unknown","agent":"vbw:vbw-dev","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: no installed skill matches this React/TypeScript/@dnd-kit + Express/Prisma/Vitest implementation tas..."}
{"ts":"2026-06-03T16:39:31Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this verification pass. No skills were preselected at orchestration time. Reason: no installed skill matches this React/TypeScript/@dnd-kit + Express/Prisma/Vitest verif..."}
{"ts":"2026-06-03T16:48:01Z","session":"unknown","agent":"vbw:vbw-lead","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: writing a QA remediation plan for a board phase; no installed skill matches this React/TS/Express/Pr..."}
{"ts":"2026-06-03T16:50:02Z","session":"unknown","agent":"vbw:vbw-dev","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this remediation task is a documentation-only plan amendment plus a remediation summary; no installe..."}
{"ts":"2026-06-03T16:54:38Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this verification pass. No skills were preselected at orchestration time. Reason: re-verifying a docs-only plan-amendment remediation round; no installed skill matches i..."}
{"ts":"2026-06-03T17:25:27Z","session":"unknown","agent":"vbw:vbw-scout","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this research task. No skills were preselected at orchestration time. Reason: this is a CSS/React scroll-behavior investigation on a board UI; no installed skill matches..."}
{"ts":"2026-06-03T17:29:29Z","session":"unknown","agent":"vbw:vbw-lead","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: planning a small frontend CSS-class fix; no installed skill matches (only VBW workflow skills exist)..."}
{"ts":"2026-06-03T17:31:05Z","session":"unknown","agent":"vbw:vbw-dev","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: small frontend CSS-class edits to a React board; no installed skill matches (only VBW workflow skill..."}
{"ts":"2026-06-03T17:55:22Z","session":"unknown","agent":"vbw:vbw-scout","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: React/TypeScript + Express/Zod archive-flow research; no installed skill matches (only VBW workflow ..."}
{"ts":"2026-06-03T18:03:17Z","session":"unknown","agent":"vbw:vbw-lead","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is a planning task for a TypeScript/React + Express code change; the available skills are meta/..."}
{"ts":"2026-06-03T18:09:19Z","session":"unknown","agent":"vbw:vbw-dev","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is mechanical TypeScript/React + Express editing verified with tsc/vitest; the available skills..."}
{"ts":"2026-06-03T18:17:27Z","session":"unknown","agent":"vbw:vbw-qa","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is QA verification of a TypeScript/React + Express contract change via build/test/grep evidence..."}
{"ts":"2026-06-03T18:27:22Z","session":"unknown","agent":"vbw:vbw-lead","kind":"orchestrator_preselection","decision":"no_activation","reason":"Evaluated installed skills for this task. No skills were preselected at orchestration time. Reason: this is remediation planning for an environmental test-concurrency known issue; available skills are..."}
```

#### `.vbw-planning/todo-details.json` (552 lines, first 30 shown)
```
{
  "schema_version": 1,
  "items": {
    "572f43e0": {
      "summary": "[UAT-DEVIATION] R01: None — documentation-only plan-amendment; no product code touched. (phase 22, see remediation/qa/round-01/R01-SUMMARY.md) (added 2026-05-29) (ref:572f43e0)",
      "context": "Accepted UAT summary deviation for phase 22-project-board-kanban-ui. Deviation: None — documentation-only plan-amendment; no product code touched.. Source plan: R01. Source: remediation/qa/round-01/R01-SUMMARY.md. UAT checkpoint: 22-UAT.md#D01.",
      "files": [
        "22-UAT.md",
        "remediation/qa/round-01/R01-SUMMARY.md"
      ],
      "added": "2026-05-29",
      "source": "uat-deviation",
      "uat_deviation": {
        "phase": "22-project-board-kanban-ui",
        "checkpoint": "D01",
        "signature": "1beb3b3a255ddf394205c76cdd91a10958a776e4fe9bf2d967d7058c6f63e1f6",
        "source_plan": "R01",
        "source_path": "remediation/qa/round-01/R01-SUMMARY.md"
      }
    },
    "8b751374": {
      "summary": "[UAT-DEVIATION] R01: None — documentation-only plan-amendment; `git diff` of `frontend/` and `backend/` is empty. (phase 22, see remediation/qa/round-01/R01-SUMMARY.md) (added 2026-05-30) (ref:8b751374)",
      "context": "Accepted UAT summary deviation for phase 22-project-board-kanban-ui. Deviation: None — documentation-only plan-amendment; `git diff` of `frontend/` and `backend/` is empty.. Source plan: R01. Source: remediation/qa/round-01/R01-SUMMARY.md. UAT checkpoint: 22-UAT.md#D02.",
      "files": [
        "22-UAT.md",
        "remediation/qa/round-01/R01-SUMMARY.md"
      ],
      "added": "2026-05-30",
      "source": "uat-deviation",
      "uat_deviation": {
```

### Active Plan
---
phase: 3
plan: "01"
title: Board checklist open access + default Report-share item
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: thorough
skills_used: []
files_modified:
  - backend/src/routes/board.ts
  - backend/src/services/boardService.ts
  - backend/scripts/backfill-checklist-report-share-item.ts
  - backend/src/routes/__tests__/boardPatchChecklistAccess.test.ts
  - backend/src/services/__tests__/defaultChecklistBackfill.test.ts
forbidden_commands: []
must_haves:
  truths:
    - "A checklist-only PATCH body (Object.keys(data) === ['checklist']) from ANY authenticated user (incl. unassigned NORMAL) returns 200 and persists the checklist; the ownership 403 is skipped only on this path."
    - "A non-owner NORMAL PATCH containing stage, stageLockedBy, stage='archived', OR a mixed checklist+other-field body still returns 403; a mixed checklist+stage body leaves the card's stored checklist UNCHANGED (rejected wholesale, never partially applied)."
    - "The Phase 11 ADMIN-only archive guard (board.ts:138) and the PM/ADMIN-only stageLockedBy guard (board.ts:159) are byte-for-byte unchanged; assigned NORMAL / PM / ADMIN checklist edits still return 200."
    - "DEFAULT_CHECKLIST ends with { label: \"Report is on client's share\", checked: false, order: 6 } as its 7th/last entry; the first six entries (Kickoff..Delivery, order 0-5) are unchanged."
    - "The backfill core is a pure exported function: re-running is idempotent (exact-label match, no duplicate), a card missing the item gets it at max(order)+1, an empty/malformed-JSON checklist is treated as [] and the item is added at order 0."
    - "No frontend files and no Prisma schema/migration files are modified; BoardCard.checklist stays free-form JSON-in-TEXT."
  artifacts:
    - path: "backend/src/routes/board.ts"
      provides: "Per-field authz: checklist-only bypass inside the if(!isManager) branch of PATCH /cards/:id"
      contains: "checklistOnly"
    - path: "backend/src/services/boardService.ts"
      provides: "New default checklist item appended to DEFAULT_CHECKLIST"
      contains: "Report is on client's share"
    - path: "backend/scripts/backfill-checklist-report-share-item.ts"
      provides: "One-off idempotent backfill script with an exported pure transform function, run via npx tsx"
      contains: "Report is on client's share"
    - path: "backend/src/routes/__tests__/boardPatchChecklistAccess.test.ts"
      provides: "8-case access matrix for PATCH /cards/:id checklist access"
      contains: "unassigned"
    - path: "backend/src/services/__tests__/defaultChecklistBackfill.test.ts"
      provides: "Tests for DEFAULT_CHECKLIST contents/upsert propagation and backfill idempotency/ordering/malformed-JSON"
      contains: "idempot"
  key_links:
    - from: "backend/scripts/backfill-checklist-report-share-item.ts"
      to: "the exported pure transform function"
      via: "script main() calls the pure function per card; the same function is imported by the test"
    - from: "backend/src/services/__tests__/defaultChecklistBackfill.test.ts"
      to: "backend/scripts/backfill-checklist-report-share-item.ts"
      via: "imports the exported pure transform function (no DB, no main() side effects)"
    - from: "backend/src/services/projectService.ts (upsertByKey)"
      to: "backend/src/services/boardService.ts DEFAULT_CHECKLIST"
      via: "JSON.stringify(DEFAULT_CHECKLIST) into new BoardCard.checklist on card creation"
---
<objective>
Two independent Board/Kanban checklist changes, backend-only.
1. Open checklist check/edit to every authenticated user: PATCH /api/board/cards/:id skips the assignment-ownership 403 ONLY when the request body touches exactly `checklist`. Any other field (stage, notes, stageLockedBy) — alone or combined with checklist — keeps today's ownership/PM-ADMIN/archive gating unchanged.
2. Add a standard `Report is on client's share` checklist item: append it to DEFAULT_CHECKLIST (default on new cards) AND backfill it onto all existing cards via a one-off idempotent `npx tsx` script whose core logic is a tested pure function.
No frontend changes (checklist UI has no role gate). No schema/migration changes (checklist is free-form JSON-in-TEXT).
</objective>
<context>
@.vbw-planning/phases/03-board-checklist-open-access-report-item/03-RESEARCH.md
The research contains exact file:line targets, the full recommended PATCH branch logic, a complete backfill-script draft, and the 8-case test matrix. Follow it closely — anchor lines were re-validated: board.ts:138 archive guard, :141 `if (!isManager)`, :142 getCard, :155 'Forbidden', :159 stageLockedBy guard, :187 emitBoardInvalidate; boardService.ts:10 DEFAULT_CHECKLIST, :16 'Delivery' order 5.
@.vbw-planning/phases/03-board-checklist-open-access-report-item/.context-lead.md
Compiled milestone/operational context: locked scope (checklist-only; notes stays gated; archive stays ADMIN-only; stage-lock stays PM/ADMIN-only), and the Phase 3 goal + success criteria.

Test harness templates (reuse, do not reinvent):
- backend/src/routes/__tests__/boardPatchArchiveGuard.test.ts — buildApp() mounts the real board.ts router with an x-test-role session-injecting middleware; withDbRetry SQLite-lock wrapper; try/finally seed/teardown.
- backend/src/routes/__tests__/boardFiles.test.ts — seeds assigned NORMAL (real TeamMember+Assignment linked to the card's project) + unassigned NORMAL; carries the schedule-isolation guard (pattern (h)) asserting seeded Assignment/TeamMember rows are never mutated.
One-off script conventions: backend/scripts/dryrun-project-dedupe.ts (doc-comment stating exact run command) and backend/prisma/backfill-zones.ts (read-modify-write loop, main().catch().finally($disconnect); NOT wired into package.json).
</context>
<tasks>
<task type="auto">
  <name>Per-field authz: checklist-only bypass in PATCH /cards/:id</name>
  <files>
    backend/src/routes/board.ts
  </files>
  <action>
Inside `router.patch('/cards/:id', ...)`, modify ONLY the `if (!isManager) { ... }` branch (currently ~lines 141-162) to add a checklist-only bypass, per the research's "Recommended branch logic":
- Compute `const sentFields = Object.keys(data);` and `const checklistOnly = sentFields.length > 0 && sentFields.every((f) => f === 'checklist');` AFTER the zod parse.
- Wrap the existing ownership work (getCard + 404, ownerUserIds Set from existing.assignments[].teamMember.userId, the `Forbidden` 403, and the `data.stageLockedBy !== undefined` PM/ADMIN 403) in `if (!checklistOnly) { ... }`. When `checklistOnly` is true the handler proceeds without the ownership check.
- Do NOT touch the archive guard at line 138 (`data.stage === 'archived' && role !== 'ADMIN'` → 403), which runs before this branch. Do NOT touch the updateData build, the drag-to-move stageLockedBy auto-pin (~line 181), boardService.updateCard, or emitBoardInvalidate('cards') at line 187.
- Keep the `sentFields.length > 0` guard so an empty body from a non-owner still falls through to the ownership check (no new hole). A missing card on the checklist-only path still 404s downstream via updateCard's P2025→404 catch — do not add a separate existence check there.
- Add a short comment noting Phase 03 checklist-only access and that any other field (alone or with checklist) keeps ownership gating.
  </action>
  <verify>
`cd backend && npx tsc --noEmit` clean. `grep -n "checklistOnly" src/routes/board.ts` shows the new predicate. Confirm the archive guard string 'Only ADMIN can archive cards' and 'Only PM or ADMIN can change stage lock' are still present and unmodified.
  </verify>
  <done>
tsc passes; checklistOnly bypass present; archive + stage-lock guards unchanged.
  </done>
</task>
<task type="auto">
  <name>Append default 'Report is on client's share' item to DEFAULT_CHECKLIST</name>
  <files>
    backend/src/services/boardService.ts
  </files>
  <action>
Append `{ label: "Report is on client's share", checked: false, order: 6 }` as the 7th entry of `DEFAULT_CHECKLIST` (after `{ label: 'Delivery', checked: false, order: 5 }` at line 16). Use a double-quoted TS string literal so the apostrophe needs no escaping. Leave the existing six entries and their orders (0-5) unchanged. No other file changes — projectService.upsertByKey (line ~98) already serializes this constant verbatim into every new BoardCard.checklist.
  </action>
  <verify>
`cd backend && npx tsc --noEmit` clean. `grep -n "Report is on client's share" src/services/boardService.ts` returns the new line with order: 6.
  </verify>
  <done>
DEFAULT_CHECKLIST has 7 entries ending with the report-share item at order 6; tsc passes.
  </done>
</task>
<task type="auto">
  <name>Idempotent backfill script with extracted pure transform</name>
  <files>
    backend/scripts/backfill-checklist-report-share-item.ts
  </files>
  <action>
Create the one-off backfill script per the research draft, but factor the core into a PURE, EXPORTED function so it can be unit-tested without a DB or main() side effects:
- Export `const NEW_ITEM_LABEL = "Report is on client's share";`.
- Export a pure function, e.g. `export function backfillChecklist(rawChecklistJson: string): { checklist: ChecklistItem[]; changed: boolean }`: JSON.parse the input; if parse throws or the result is not an array, treat as `[]`; if an item with `label === NEW_ITEM_LABEL` already exists, return `{ checklist: <unchanged>, changed: false }`; otherwise push `{ label: NEW_ITEM_LABEL, checked: false, order: items.length ? Math.max(...items.map(i=>i.order))+1 : 0 }` and return `{ ..., changed: true }`.
- `main()` does `prisma.boardCard.findMany({ select: { id: true, checklist: true } })`, calls `backfillChecklist(card.checklist)` per row, and only `prisma.boardCard.update(...)` when `changed` is true; console.log per-update and a final `Updated N, skipped M` summary.
- Ensure importing the module for tests does NOT execute the DB loop: guard the invocation so `main()` only runs when the file is the entrypoint (e.g. compare `import.meta.url` to `process.argv[1]`), then `.catch(err => { console.error(...); process.exit(1); }).finally(() => prisma.$disconnect())`. Do NOT wire into package.json.
- Doc-comment the exact run command: `npx tsx backend/scripts/backfill-checklist-report-share-item.ts` and note idempotency (exact-label, case-sensitive). Import prisma via `../src/db/prisma.js` to match repo convention.
  </action>
  <verify>
`cd backend && npx tsc --noEmit` clean. `grep -n "export function backfillChecklist\|NEW_ITEM_LABEL\|import.meta.url" scripts/backfill-checklist-report-share-item.ts` all present. Confirm no `"backfill"` entry was added to package.json.
  </verify>
  <done>
Script exports a pure `backfillChecklist`, guards main() behind an entrypoint check, is not wired into package.json, and tsc passes.
  </done>
</task>
<task type="auto">
  <name>Tests: access matrix + default/backfill behavior</name>
  <files>
    backend/src/routes/__tests__/boardPatchChecklistAccess.test.ts
    backend/src/services/__tests__/defaultChecklistBackfill.test.ts
  </files>
  <action>
Add TWO test files.
(A) boardPatchChecklistAccess.test.ts — model on boardPatchArchiveGuard.test.ts (buildApp + x-test-role middleware + withDbRetry) and boardFiles.test.ts (assigned/unassigned NORMAL seeding via real TeamMember+Assignment, plus the schedule-isolation guard). Cover the 8-case matrix from research §"Existing tests to extend":
  1. Unassigned NORMAL PATCH { checklist:[...] } → 200, checklist persisted in DB.
  2. Unassigned NORMAL PATCH { stage:'execution' } → 403.
  3. Unassigned NORMAL PATCH { stageLockedBy:'x' } → 403.
  4. Unassigned NORMAL PATCH { checklist:[...], stage:'execution' } → 403 AND assert the card's stored checklist is UNCHANGED (not partially applied).
  5. Unassigned NORMAL PATCH { stage:'archived' } → 403 'Only ADMIN can archive cards' (Phase 11 guard regression).
  6. Assigned NORMAL PATCH { checklist:[...] } → 200 (happy path unaffected).
  7. PM and ADMIN PATCH { checklist:[...] } → 200 (regression).
  8. Schedule-isolation guard: after the suite, seeded Assignment/TeamMember rows are untouched (reuse boardFiles.test.ts pattern (h)).
(B) defaultChecklistBackfill.test.ts — import DEFAULT_CHECKLIST from boardService and `backfillChecklist` from the script:
  - Assert DEFAULT_CHECKLIST has 7 entries and the last is exactly { label:"Report is on client's share", checked:false, order:6 } (guards against silent future edits).
  - backfillChecklist on a checklist missing the item → changed:true, item appended at max(order)+1.
  - backfillChecklist on a checklist already containing the item → changed:false, array unchanged (idempotency).
  - backfillChecklist on '' and on malformed JSON (e.g. '{oops') and on a non-array JSON value → treated as [], item added at order 0.
  - (Optional but preferred, thorough) projectService.upsertByKey on a brand-new dedupe key creates a BoardCard whose parsed checklist includes the report-share item as the last entry.
  </action>
  <verify>
`cd backend && npx vitest run src/routes/__tests__/boardPatchChecklistAccess.test.ts src/services/__tests__/defaultChecklistBackfill.test.ts` — all cases green. Then run the full board/service suites to confirm no regression: `npx vitest run src/routes/__tests__ src/services/__tests__`.
  </verify>
  <done>
Both new test files pass; existing board/service suites still pass; the mixed-body case proves the checklist is left unchanged on 403.
  </done>
</task>
</tasks>
<verification>
1. `cd backend && npx tsc --noEmit` is clean.
2. `cd backend && npx vitest run src/routes/__tests__ src/services/__tests__` — all green, including the new access-matrix and default/backfill tests.
3. No-frontend / no-schema confirmation (verify step, not a code change): `git status` shows changes only under backend/src/routes/board.ts, backend/src/services/boardService.ts, backend/scripts/, and the two new test files — NO files under frontend/ and NO files under backend/prisma/ (schema/migrations).
4. Grep guards: `grep -rn "checklistOnly" backend/src/routes/board.ts`; `grep -rn "Report is on client's share" backend/src/services/boardService.ts backend/scripts/`; confirm 'Only ADMIN can archive cards' and 'Only PM or ADMIN can change stage lock' strings are still present in board.ts.
5. One-time operational step (run once after code merges, DEV database only — this is a data operation, not a commit): `cd backend && npx tsx scripts/backfill-checklist-report-share-item.ts`. Then verify with a READ-ONLY check that every card now contains the item exactly once, and prove idempotency by running the script a second time and confirming the summary reports `Updated 0` (all skipped) and no duplicate labels. Do NOT run any destructive DB command; the script only appends via per-row updates guarded by the exact-label idempotency check.
</verification>
<success_criteria>
- An unassigned NORMAL user can PATCH /api/board/cards/:id with a checklist-only body and the change persists (200).
- A non-owner NORMAL user is still 403'd for stage, stageLockedBy, and stage='archived'; a mixed checklist+stage body is rejected and the card's checklist is left unchanged.
- ADMIN-only archive and PM/ADMIN-only stage-lock guards remain enforced; assigned NORMAL/PM/ADMIN checklist edits still work.
- Newly created project cards include a `Report is on client's share` checklist item (DEFAULT_CHECKLIST, order 6).
- After the backfill runs, every existing card's checklist contains the item exactly once; re-running adds no duplicates (idempotent pure function, exact-label match, max(order)+1 ordering, malformed JSON treated as empty).
- Backend tsc build and the board/service test suites pass, with new tests covering the access matrix and the default/backfill behavior. No frontend or schema/migration changes.
</success_criteria>
<output>
03-01-SUMMARY.md
</output>
</content>
</invoke>

### Research Findings
---
phase: "03"
title: "Board checklist open access + default 'Report is on client's share' item"
type: research
confidence: high
date: 2026-07-02
---

# Research — Board Checklist Open Access & Default Report-Share Item

## Change 1 — Checklist Access

### Current authz logic (exact)

File: `backend/src/routes/board.ts`

- Zod body schema (lines 122-127):
  ```ts
  const schema = z.object({
    stage: StageEnum.optional(),
    notes: z.string().optional(),
    checklist: z.array(ChecklistItemSchema).optional(),
    stageLockedBy: z.string().nullable().optional(),
  });
  ```
  `ChecklistItemSchema` (lines 35-39): `{ label: z.string(), checked: z.boolean(), order: z.number() }`.
  Because every field is `.optional()` and the body arrives as JSON, `Object.keys(data)` after `schema.parse(req.body)` reflects **exactly** the fields the client actually sent (zod does not backfill absent optional keys with an `undefined` own-property).

- Handler `router.patch('/cards/:id', requireAuth, mutationRateLimiter, ...)` (line 120):
  1. `role = req.session.role`; `isManager = role === 'PM' || role === 'ADMIN'` (lines 130-131).
  2. **Archive guard (NON-NEGOTIABLE, Phase 11)** — line 137: `if (data.stage === 'archived' && role !== 'ADMIN') return 403 'Only ADMIN can archive cards'`. Runs unconditionally, before the manager/ownership branch. **Must stay exactly as-is.**
  3. **Ownership branch** — `if (!isManager)` (lines 141-162):
     - Fetches `existing = await boardService.getCard(id)` (404 if missing).
     - Builds `ownerUserIds` from `existing.assignments[].teamMember.userId` (Phase 24-R03 shape — one Project can have several Assignments/pentesters).
     - `if (!ownerUserIds.has(req.session.userId ?? '')) return 403 'Forbidden'` — **this is the line that blocks unassigned NORMAL users from ANY field**, including `checklist`.
     - `if (data.stageLockedBy !== undefined) return 403 'Only PM or ADMIN can change stage lock'` — **must stay restricted to PM/ADMIN.**
  4. Builds `updateData` (lines 164-183), sets `archivedAt` on `stage==='archived'`, and auto-pins `stageLockedBy` to the acting user when a bare `stage` move is sent without an explicit `stageLockedBy` (drag-to-move semantics) — unaffected by this change since it only fires when `data.stage !== undefined`.
  5. `boardService.updateCard(id, updateData)` → `res.json({ card })` → `emitBoardInvalidate('cards')` (line 187).

**Which fields are gated today:** ALL four optional fields (`stage`, `notes`, `checklist`, `stageLockedBy`) are gated by the same single ownership check for NORMAL users — there is no per-field branching today. `stage='archived'` is additionally ADMIN-only regardless of manager status. `stageLockedBy` is additionally PM/ADMIN-only even for owning NORMAL users.

### Recommended branch logic

Goal: a NORMAL non-owner sending a body that contains **only** `checklist` should skip the ownership 403; any other field present (alone or combined with `checklist`) keeps today's ownership-gated behavior untouched. The archive guard (step 2 above) is unconditional and already unaffected.

```ts
if (!isManager) {
  // Phase 03: any authenticated user may check/uncheck checklist items on
  // ANY project's card. Requests that touch ONLY `checklist` skip the
  // ownership check entirely; any other field (stage, notes, stageLockedBy)
  // — alone or combined with checklist — still requires ownership so the
  // existing PM/ADMIN-only and archive-only guards are untouched.
  const sentFields = Object.keys(data);
  const checklistOnly = sentFields.length > 0 && sentFields.every((f) => f === 'checklist');

  if (!checklistOnly) {
    const existing = await boardService.getCard(id);
    if (!existing) {
      return res.status(404).json({ error: 'Card not found' });
    }
    const ownerUserIds = new Set(
      existing.assignments
        .map((a) => a.teamMember?.userId)
        .filter((u): u is string => !!u),
    );
    if (!ownerUserIds.has(req.session.userId ?? '')) {
      return res.status(403).json({ error: 'Forbidden' });
    }
    if (data.stageLockedBy !== undefined) {
      return res.status(403).json({ error: 'Only PM or ADMIN can change stage lock' });
    }
  }
  // checklistOnly === true: any authenticated user proceeds. A missing card
  // still 404s downstream via boardService.updateCard's Prisma P2025 → the
  // existing catch block (line 192-194) already converts that to 404, so no
  // separate existence check is needed on this path.
}
```

Notes for the planner:
- `sentFields.length > 0` guard preserves current behavior for an **empty** body from a non-owner (falls through to the ownership check exactly as today — no new hole).
- A body of `{ checklist: [...], notes: '...' }` from a non-owner is **not** `checklistOnly` and correctly falls back to the ownership-gated path — this is a deliberate, conservative choice: the task only asked to open `checklist`, not `notes`. Flagged as an explicit decision below (Open Questions).
- `{ checklist: [...], stage: 'execution' }` and `{ checklist: [...], stageLockedBy: 'x' }` from a non-owner also correctly fall back to ownership/PM-ADMIN gating — satisfies the requirement's example exactly.
- The archive guard at line 137 and the PM-drag-to-archive block are **completely unmodified** by this change — they run before this branch and don't depend on it.
- No change needed to `boardService.updateCard` (`backend/src/services/boardService.ts:177-200`) — it already accepts a partial `checklist`-only `updateData` object.

### Frontend gating

Searched `frontend/src/features/board/` for RBAC/assignment gating around the checklist:

- `frontend/src/features/board/components/CardDetailModal.tsx` lines 588-628 render the checklist and wire the toggle button (`onClick` → `updateCard.mutate({ id: card.id, data: { checklist: next } })`, line 604-609). **There is no role or assignment check anywhere in this block** — no `canEditChecklist`, no `hasRole()`, no `assignments.some(...)` guard. The only gated affordances in this file are `canDelete = role === 'ADMIN' || role === 'PM'` (line 486) and `canArchive = role === 'ADMIN' && !card.archivedAt'` (line 487), used solely for the Delete/Archive footer buttons — checklist is untouched by either.
- `frontend/src/features/board/components/KanbanCard.tsx` only renders a read-only `checkedCount/totalCount` badge (lines 122-123, 183-218); no interaction, no gating.
- `frontend/src/features/board/components/BoardFilters.tsx:98` has the only `hasRole('PM')` gate in the board feature slice, and it's for a filter control, unrelated to checklist.
- `GET /api/board/cards` and `GET /api/board/cards/:id` are `requireAuth`-only (board.ts lines 60, 93 — "all authenticated users" per their own doc-comments), so any authenticated user can already open `CardDetailModal` for any card and see/click the checklist.

**Conclusion: no frontend changes are required for Change 1.** The UI is already fully open; only the backend 403 was blocking unassigned NORMAL users. Once the backend branch above ships, the existing UI becomes usable end-to-end with zero frontend edits. (Worth a quick manual check post-implementation that `useUpdateCard`'s existing `onError: handleMutationError` toast, `frontend/src/features/board/hooks.ts:57`, still fires correctly for the genuinely-still-blocked cases like stage changes by non-owners — no code change expected there either.)

### Socket.IO broadcast coverage

- `backend/src/routes/board.ts:187` calls `emitBoardInvalidate('cards')` **unconditionally on every successful PATCH**, including checklist-only updates — this line is outside any role/ownership branch.
- `backend/src/services/socketService.ts:17-19`: `emitBoardInvalidate` does `_io?.emit('board:invalidate', { resource })` — a **global broadcast**, no room/user scoping. Every connected socket receives it.
- `frontend/src/features/board/useBoardSync.ts:21-26` listens for `board:invalidate` and calls `queryClient.invalidateQueries({ queryKey: ['board', resource] })` for any connected client, regardless of role or assignment.

**Conclusion: realtime propagation already fully covers this change. No changes needed to `socketService.ts`, `useBoardSync.ts`, or the emit call site.**

### Existing tests to extend

- `backend/src/routes/__tests__/boardPatchArchiveGuard.test.ts` — covers only the PM/ADMIN archive-stage guard (PM 403 on `stage=archived`, ADMIN 200, PM 200 on non-archived stage). It does **not** exercise NORMAL role or ownership at all. Good template for harness reuse: builds a real Express app mounting the actual `board.ts` router with a session-injecting middleware keyed off an `x-test-role` header (see `buildApp`, lines 82-96), plus a `withDbRetry` SQLite-lock-retry wrapper and try/finally seed/teardown.
- `backend/src/routes/__tests__/boardFiles.test.ts` — closer template for the ownership dimension: seeds **two NORMAL users**, "assigned" (via a real `TeamMember` + `Assignment` linked to the card's project) and "unassigned" (comment header lines 7-25, seed code ~139-200, session-injection ~340-346). It also carries a schedule-isolation regression test (`(h)` around line 442) asserting the seeded `Assignment`/`TeamMember` rows are never mutated by the suite — reuse that pattern for any new checklist-access test file since board fixtures now touch `Assignment`/`TeamMember`.
- No dedicated test file exists yet for card-PATCH ownership on NORMAL users (`checklist`, `notes`, `stage` as a non-owner) — this is a coverage gap the plan should close. Recommend a new file, e.g. `backend/src/routes/__tests__/boardPatchChecklistAccess.test.ts`, combining both templates:
  1. Seed: assigned NORMAL (via `TeamMember`+`Assignment`, mirroring `boardFiles.test.ts`), unassigned NORMAL, PM, ADMIN, and a card.
  2. `PATCH /cards/:id { checklist: [...] }` as **unassigned NORMAL** → expect 200, checklist persisted.
  3. `PATCH /cards/:id { stage: 'execution' }` as **unassigned NORMAL** → expect 403 (existing behavior, regression guard).
  4. `PATCH /cards/:id { stageLockedBy: 'x' }` as **unassigned NORMAL** → expect 403.
  5. `PATCH /cards/:id { checklist: [...], stage: 'execution' }` as **unassigned NORMAL** → expect 403, and assert the card's `checklist` in the DB is **unchanged** (the mixed request must be rejected wholesale, not partially applied).
  6. `PATCH /cards/:id { stage: 'archived' }` as unassigned NORMAL → still 403 'Only ADMIN can archive cards' (regression guard for the Phase 11 guard, unaffected by this change).
  7. Regression: assigned NORMAL, PM, ADMIN can still PATCH `checklist` (200) — happy-path unaffected.
  8. Schedule-isolation guard (per `boardFiles.test.ts` pattern (h)): assert the seeded `Assignment`/`TeamMember` rows are untouched after the suite runs.

## Change 2 — Default "Report on client's share" Item

### Where checklist is first populated

- `backend/src/services/boardService.ts:10-17` — `DEFAULT_CHECKLIST` constant:
  ```ts
  export const DEFAULT_CHECKLIST: ChecklistItem[] = [
    { label: 'Kickoff', checked: false, order: 0 },
    { label: 'Requirements', checked: false, order: 1 },
    { label: 'Pentest', checked: false, order: 2 },
    { label: 'Report', checked: false, order: 3 },
    { label: 'Review', checked: false, order: 4 },
    { label: 'Delivery', checked: false, order: 5 },
  ];
  ```
- `backend/src/services/projectService.ts:1-2,88-104` — `upsertByKey()` is the **only** production code path that creates a `BoardCard`. On a genuinely new Project (no existing row matching the `(name, clientId, sortedTags)` dedupe triple), it does:
  ```ts
  return prisma.project.create({
    data: {
      ...
      boardCard: {
        create: {
          stage: 'upcoming',
          checklist: JSON.stringify(DEFAULT_CHECKLIST),   // line 98
          notes: '',
        },
      },
    },
  });
  ```
  `upsertByKey` is called from `assignmentService.linkProjectsForAssignment` (per the `board.ts:80-86` comment) whenever an assignment links to a Project — this is the single "new card" creation trigger. Cards are never created bare/empty; they always start with `DEFAULT_CHECKLIST`, never `[]`.
- All other `prisma.boardCard.create(...)` call sites are **test fixtures only** (`scheduleIsolation.phase23.test.ts:109`, `deleteAssignmentOrphanFailure.test.ts:136`, `boardAutoMove.stopped.test.ts:112,137`, `boardCardDelete.pm.test.ts:178`, `boardFiles.test.ts:212,222`, `boardAdminArchive.test.ts:133`, `boardPatchArchiveGuard.test.ts:131`) — none of these are production paths and don't need to be touched for the default-item change, though any test asserting the exact shape/length of `DEFAULT_CHECKLIST` (none currently do) would need re-checking after the edit.
- Prisma schema: `backend/prisma/schema.prisma:315-341` — `BoardCard.checklist String @default("[]")` (DB-level default is just an empty-array string; the *real* default population happens in application code via `DEFAULT_CHECKLIST`, not the DB column default — the column default only matters for the rare raw-insert path, which doesn't exist in this codebase).

### Checklist item shape (confirmed)

From `ChecklistItemSchema` (`backend/src/routes/board.ts:35-39`) and the `boardService.ts:4-8` `ChecklistItem` interface (both identical) and the frontend mirror (`frontend/src/features/board/types.ts:3-7`):
```ts
{ label: string; checked: boolean; order: number }
```
New default item: `{ label: "Report is on client's share", checked: false, order: 6 }` (appended after the existing `order: 5` "Delivery" item — see ordering discussion below). The apostrophe in the label needs no special escaping — `JSON.stringify`/zod `z.string()` handle it natively; this is unrelated to the YAML-quote-escaping concern noted elsewhere in this project's memory (that applies to YAML frontmatter, not JSON-in-TEXT columns or TS string literals).

### Recommended implementation — DEFAULT part (new cards)

Single-line change to `backend/src/services/boardService.ts`:
```ts
export const DEFAULT_CHECKLIST: ChecklistItem[] = [
  { label: 'Kickoff', checked: false, order: 0 },
  { label: 'Requirements', checked: false, order: 1 },
  { label: 'Pentest', checked: false, order: 2 },
  { label: 'Report', checked: false, order: 3 },
  { label: 'Review', checked: false, order: 4 },
  { label: 'Delivery', checked: false, order: 5 },
  { label: "Report is on client's share", checked: false, order: 6 },
];
```
Because `projectService.upsertByKey` (line 98) serializes this constant verbatim into every newly-created `BoardCard.checklist`, this one edit is sufficient for the "default on new cards" half — no other file needs to change for that half.

### Recommended implementation — BACKFILL part (existing cards)

Model on `backend/prisma/backfill-zones.ts` (read-modify-write loop over all rows, one-by-one `console.log`-annotated updates, `main().catch(...).finally(prisma.$disconnect())` — no npm script wiring; run manually via `npx tsx`) and `backend/scripts/dryrun-project-dedupe.ts` (doc-comment stating exact run command and safety guarantees).

Proposed new file: `backend/scripts/backfill-checklist-report-share-item.ts` (co-located with the other one-off `backend/scripts/*.ts`, since this touches `BoardCard` not a Prisma-migration-adjacent concern like `backfill-zones.ts` does for `TemplateMapping`).

```ts
/**
 * Backfill: append the "Report is on client's share" checklist item to every
 * existing BoardCard that doesn't already have it.
 *
 * Idempotent — safe to re-run. Skips any card whose checklist already
 * contains an item with this exact label (case-sensitive exact match).
 *
 * Run with: npx tsx backend/scripts/backfill-checklist-report-share-item.ts
 */
import { prisma } from '../src/db/prisma.js';

const NEW_ITEM_LABEL = "Report is on client's share";

interface ChecklistItem {
  label: string;
  checked: boolean;
  order: number;
}

async function main(): Promise<void> {
  const cards = await prisma.boardCard.findMany({
    select: { id: true, checklist: true },
  });

  let updated = 0;
  let skipped = 0;

  for (const card of cards) {
    let items: ChecklistItem[];
    try {
      items = JSON.parse(card.checklist) as ChecklistItem[];
      if (!Array.isArray(items)) items = [];
    } catch {
      // Malformed JSON-in-TEXT — treat as empty rather than crash the run.
      items = [];
    }

    if (items.some((i) => i.label === NEW_ITEM_LABEL)) {
      skipped++;
      continue;
    }

    const nextOrder = items.length
      ? Math.max(...items.map((i) => i.order)) + 1
      : 0;
    items.push({ label: NEW_ITEM_LABEL, checked: false, order: nextOrder });

    await prisma.boardCard.update({
      where: { id: card.id },
      data: { checklist: JSON.stringify(items) },
    });
    updated++;
    console.log(`[backfill] card ${card.id}: added "${NEW_ITEM_LABEL}" at order ${nextOrder}`);
  }

  console.log(`[backfill] Done. Updated ${updated}, skipped ${skipped} (already had it).`);
}

main()
  .catch((err) => {
    console.error('[backfill] Fatal error:', err);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

Why not a startup routine: `backend/src/index.ts`'s `startServer()` (lines 121-200+) has no precedent for running a data backfill on boot — all one-off migrations in this repo (`backfill-zones.ts`, `dryrun-project-dedupe.ts`) are manual, explicitly-invoked scripts. Running this at every boot would re-scan every `BoardCard` on every restart (harmless given the idempotency guard, but wasteful and inconsistent with repo convention) and would need extra care under multi-instance deploys. Recommend the manual-script approach; the plan should note it as a one-time operational step to run once after the code change ships (e.g. documented in the phase's task list or DEPLOYMENT-GUIDE.md, not automated).

### Idempotency + ordering

- **Idempotency guard:** exact-label match against `NEW_ITEM_LABEL` (`items.some((i) => i.label === NEW_ITEM_LABEL)`) before appending — re-running the script is a no-op for already-backfilled cards. This also protects against the (unlikely) race of a card created by `upsertByKey` *after* the DEFAULT_CHECKLIST edit ships but *before* the backfill runs — such a card already has the item from creation, so the backfill correctly skips it.
- **Ordering:** compute `nextOrder = Math.max(...existing orders) + 1` per card rather than hardcoding `6`, since a card's checklist may have been user-edited (items reordered, deleted, or added via other means) and no longer matches `DEFAULT_CHECKLIST` verbatim by the time the backfill runs. This keeps the new item strictly after every existing item on that specific card regardless of drift from the default set.
- **New-card path** does not need the same defensive `Math.max` logic — `DEFAULT_CHECKLIST` is a static literal appended once in source, so `order: 6` is correct and stable there (as long as no earlier entries in the same array are added/removed later without renumbering — flag this as a "keep in sync" note for future edits to `DEFAULT_CHECKLIST`).
- **Sort-order rendering:** `CardDetailModal.tsx:599` (`card.checklist.sort((a, b) => a.order - b.order)`) already sorts by `order` at render time, so the appended item naturally renders last regardless of insertion order in the JSON array.

### Tests to add/extend

- No existing test asserts `DEFAULT_CHECKLIST`'s exact contents or length. Add/extend a test (new or in an existing `projectService`/`boardService` test file — none currently exist under `backend/src/services/__tests__/` for these two services specifically, which is itself a gap per `TESTING.md`'s "Gaps/risks" note) asserting:
  1. `projectService.upsertByKey(...)` on a brand-new dedupe key creates a `BoardCard` whose parsed `checklist` includes `{ label: "Report is on client's share", checked: false, order: 6 }` as the last entry.
  2. The full `DEFAULT_CHECKLIST` array shape/order is as expected (guards against accidental future edits silently changing default items).
- For the backfill script (new, no existing coverage — `backend/scripts/*.ts` one-offs have no test files in this repo currently, consistent with `dryrun-project-dedupe.ts`/`backfill-zones.ts` having none either), consider either:
  - A lightweight Vitest that imports and calls the script's exported `main()`-equivalent logic against seeded `BoardCard` rows (assert: card with existing item is skipped/unchanged; card without it gets the item appended at `max(order)+1`; re-running twice is idempotent), or
  - If the plan prefers to match repo convention (no tests for one-off scripts), document manual verification steps instead and flag it as `⚠ REQUIRES MANUAL VALIDATION` for Dev/Debugger post-backfill (e.g., spot-check a handful of `BoardCard.checklist` rows via Prisma Studio or a read-only query before/after running the script).

## Recommended Approach

Concrete file list and what changes in each:

1. **`backend/src/routes/board.ts`** (Change 1) — inside `router.patch('/cards/:id', ...)`, replace the `if (!isManager) { ... }` block (lines 141-162) with the `checklistOnly`-aware version shown above. No other route in this file changes. The archive guard (line 137) and the `emitBoardInvalidate('cards')` call (line 187) are untouched.
2. **`backend/src/services/boardService.ts`** (Change 2) — append one entry to `DEFAULT_CHECKLIST` (lines 10-17): `{ label: "Report is on client's share", checked: false, order: 6 }`.
3. **`backend/scripts/backfill-checklist-report-share-item.ts`** (Change 2, new file) — one-off idempotent backfill script per the template above, run manually once via `npx tsx backend/scripts/backfill-checklist-report-share-item.ts` after deploy.
4. **`backend/src/routes/__tests__/boardPatchChecklistAccess.test.ts`** (Change 1, new file) — new test suite per the 8-case list above, modeled on `boardPatchArchiveGuard.test.ts` (harness) + `boardFiles.test.ts` (assigned/unassigned NORMAL seeding + schedule-isolation guard).
5. **A `boardService`/`projectService` test** (Change 2, new or extended file under `backend/src/services/__tests__/`) — asserts `DEFAULT_CHECKLIST` contents and that `upsertByKey` propagates the new item into freshly-created cards.
6. **No frontend changes required.** `frontend/src/features/board/components/CardDetailModal.tsx` already renders and lets any authenticated user click any checklist item with no role/assignment gate; `frontend/src/features/board/types.ts`'s `ChecklistItem` shape already matches; Socket.IO sync (`useBoardSync.ts`) already invalidates for every connected client on any `board:invalidate('cards')` emit.
7. **No schema/migration change required.** `BoardCard.checklist` is already a free-form JSON-in-TEXT column (`schema.prisma:323`); adding an item is a data-shape change, not a schema change.

## Open Questions / Decisions

1. **Should `notes` also be opened to non-owners, or only `checklist`?** The task explicitly scopes this to checklist; the recommended branch logic keeps `notes` ownership-gated (a body with both `checklist` and `notes` from a non-owner falls back to the ownership check and 403s). Confirm this is the intended scope before implementation — if `notes` should also open up, the `checklistOnly` predicate would need to become `['checklist', 'notes'].includes(f)` instead, but nothing in the phase framing asked for that.
2. **Position of the new default item.** Recommended: append at the end (`order: 6`, after "Delivery"). An alternative would be inserting it adjacent to "Report" (`order: 3.5`-style renumber) since it's semantically report-related, but that requires renumbering every subsequent default item and complicates the backfill's `Math.max`-based ordering for cards that already deviate from the default set. Recommend keeping the simpler append-at-end approach unless the user has a strong opinion on visual position.
3. **Where the backfill script lives and how it's invoked.** Recommended `backend/scripts/backfill-checklist-report-share-item.ts`, manual `npx tsx` invocation, no npm script entry — matches `dryrun-project-dedupe.ts` exactly. If the team wants it wired into `package.json` (e.g. `"backfill:checklist-report-share": "tsx scripts/backfill-checklist-report-share-item.ts"`), that's a small addition but not currently the repo's convention for one-off backfills (`backfill-zones.ts` isn't wired either).
4. **Test coverage for the backfill script itself.** Repo convention is to leave one-off scripts untested (neither `backfill-zones.ts` nor `dryrun-project-dedupe.ts` has a test file). The plan should explicitly decide whether Change 2's backfill gets a Vitest or just documented manual verification — flagged above as `⚠ REQUIRES MANUAL VALIDATION` if the latter is chosen.
5. **Exact-label idempotency match is case- and whitespace-sensitive.** If a future edit changes the label text (e.g. punctuation), the backfill's `items.some((i) => i.label === NEW_ITEM_LABEL)` guard would no longer recognize already-backfilled cards as done and would append a near-duplicate. Not a concern for this phase (label is fixed), but worth noting as a fragility if the label ever needs a wording tweak — a future backfill would need its own new idempotency key or a migration/dedupe pass.

## Live Validation Evidence

No live DB/HTTP validation was performed — this phase's research is explicitly static (per task instructions: "This is static research — no live DB/HTTP validation needed"). All findings above are derived from direct reads of `backend/src/routes/board.ts`, `backend/src/services/boardService.ts`, `backend/src/services/projectService.ts`, `backend/prisma/schema.prisma`, `backend/prisma/backfill-zones.ts`, `backend/scripts/dryrun-project-dedupe.ts`, the frontend `features/board/` slice, and the existing test files named above — all read in full or by targeted line range during this research pass.
