---
phase: 10
plan: "01"
title: Planner card client name — bold + client colour (with light-colour legibility guard)
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: balanced
skills_used: []
files_modified:
  - frontend/src/features/board/components/KanbanCard.tsx
  - frontend/src/features/board/components/__tests__/KanbanCard.test.tsx
forbidden_commands:
  - "prisma migrate"
  - "prisma db push"
  - "npx prisma *"
must_haves:
  truths:
    - "FRONTEND-ONLY (NON-NEGOTIABLE): the ONLY product files touched are frontend/src/features/board/components/KanbanCard.tsx and its co-located test. NO backend change (boardService already selects client.color), NO Prisma migration (Client.color is a pre-existing non-null column — backend/prisma/schema.prisma and the migrations dir are byte-for-byte unchanged), NO new API/hook."
    - "SCOPE = CARD PREVIEW ONLY: CardDetailModal.tsx is NOT modified, no Schedule file is modified (AssignmentCell.tsx / schedule utils / schedule types untouched), and the shared primitive frontend/src/components/ui/avatar.tsx is NOT modified. The change is confined to the Row-2 client-name <p> and the memo comparator inside KanbanCard.tsx."
    - "SCHEDULE ISOLATION (milestone-wide, NON-NEGOTIABLE): no writes to Assignment/TeamMember/Absence/Holiday and NO import from any frontend/src/features/schedule/** module. The luminance/contrast guard (see below) is a small LOCAL helper inside KanbanCard.tsx — it must NOT import the schedule-private getContrastColor (which lives un-exported inside AssignmentCell.tsx / exportHtml.ts); duplicating ~4 lines locally is required to keep the board decoupled from the schedule."
    - "BOLD + CLIENT COLOUR: the Row-2 client-name <p> (currently `text-xs text-muted-foreground` at ~L167) renders the name with a bold/semibold weight (Tailwind `font-bold` or `font-semibold`) AND applies the client colour as inline text colour via `style={{ color: <resolved colour> }}`. The `text-muted-foreground` class is dropped on the coloured path (it would override/grey the inline colour)."
    - "LEGIBILITY GUARD (chosen approach — luminance fallback): a local pure helper in KanbanCard.tsx computes the relative luminance of card.project.client.color (formula `(0.299*r + 0.587*g + 0.114*b)/255`, matching the schedule's own threshold convention) and, when the colour is too light to read on the white `bg-card` (luminance above a documented threshold, e.g. > 0.7), falls back to a readable colour (a dark token such as the existing muted-foreground / `#1a1a1a`-style dark) for the text rather than rendering an illegible pale hex. Mid/dark client colours render as their own hex. This decision is implemented and asserted, NOT merely commented."
    - "GRACEFUL FALLBACK: if card.project.client is null/undefined OR client.color is missing/empty, the name renders without crashing — the colour-resolution helper returns a safe readable default and the component never passes `undefined`/`''` into a way that throws. The existing `card.project.client?.name &&` guard around the row is preserved (no client name -> row not rendered, no null-colour access)."
    - "MEMO COMPARATOR: the KanbanCard custom memo comparator (~L228) gains `prev.card.project.client?.color === next.card.project.client?.color &&` (alongside the existing `client?.name` line) so a live client-colour edit (React Query refetch / socket invalidate) re-renders the card instead of being memoized stale."
    - "DEVN-05 is accepted and non-blocking: do NOT attempt to fix or re-open the pre-existing accepted KanbanCard ESLint finding; only the new lines must be clean."
    - "GREEN SUITE: `npx vitest run` (frontend) passes including the existing KanbanCard tests (e.g. test (d) still finds 'Acme Corp') plus the new bold+colour and light-colour-fallback assertions, and `tsc -b` (or `npx tsc --noEmit`) reports no new type errors."
  artifacts:
    - path: "frontend/src/features/board/components/KanbanCard.tsx"
      provides: "Row-2 client-name <p> rendered bold + in the client's colour via inline style, with a local luminance guard that substitutes a readable dark colour when the client hex is too light for the white card; a null/empty-colour safe default; and the memo comparator extended with the client.color guard."
      contains: "style={{ color"
    - path: "frontend/src/features/board/components/KanbanCard.tsx"
      provides: "Memo comparator client-colour guard so a colour edit re-renders the card."
      contains: "client?.color === next.card.project.client?.color"
    - path: "frontend/src/features/board/components/__tests__/KanbanCard.test.tsx"
      provides: "New assertions: (1) client name is bold and carries the client colour as inline style for a mid/dark colour; (2) a pale client colour falls back to a readable dark colour (not the pale hex); (3) missing/empty client.color renders the name without crashing."
      contains: "font"
  key_links:
    - from: "frontend/src/features/board/components/KanbanCard.tsx"
      to: "frontend/src/features/board/types.ts"
      via: "reads card.project.client.color (BoardCard.project.client.color: string) — type already present, no type change needed"
---
<objective>
On the planner/board Kanban card preview, render the client name in bold and in the
client's own colour (Client.color, already in every board payload). Apply a small local
legibility guard so a pale client colour falls back to a readable dark colour on the white
card background instead of rendering illegibly. Frontend-only, single component + its test —
no backend change, no Prisma migration, no Schedule/CardDetailModal/avatar changes.
</objective>
<context>
@.vbw-planning/phases/10-planner-client-name-style/10-RESEARCH.md
@.vbw-planning/codebase/CONVENTIONS.md
@.vbw-planning/codebase/PATTERNS.md
@.vbw-planning/codebase/TESTING.md
Render site is KanbanCard.tsx Row 2 (~L166-168): `{card.project.client?.name && (<p className="text-xs text-muted-foreground">{card.project.client.name}</p>)}`.
The accent bar at ~L150 already uses a raw client/project hex inline (`style={{ backgroundColor: card.project.color }}`) — the inline-style-from-hex pattern is house style here.
The schedule's getContrastColor (AssignmentCell.tsx:119, exportHtml.ts:32) is a PRIVATE un-exported function and lives in the isolated schedule feature — do NOT import it. Its luminance formula `(0.299*r + 0.587*g + 0.114*b)/255` is the convention to mirror in a small LOCAL board-side helper; note its job (pick fg given a bg) is the inverse of ours (decide whether a client hex is too light to use AS text on white), so reuse the formula, not the function.
The test makeCard() already seeds `client: { id, name: 'Acme Corp', color: '#3366ff' }`; tests render via RTL inside <DndContext> and assert on text content and inline `.style` (see fallbackBgColors helper reading `.style.backgroundColor`). Mirror that to read the client-name <p>'s `.style.color` and computed font-weight.
DEVN-05: a pre-existing accepted KanbanCard ESLint finding — non-blocking, do not touch.
</context>
<tasks>
<task type="auto">
  <name>Style client name bold + client colour with luminance legibility guard and memo comparator update</name>
  <files>
    frontend/src/features/board/components/KanbanCard.tsx
  </files>
  <action>
Add a small LOCAL pure helper (module-scope in KanbanCard.tsx, NOT imported from schedule)
that resolves the client-name text colour from a hex:
  - Parse the hex to r,g,b; compute luminance = (0.299*r + 0.587*g + 0.114*b) / 255 (mirror
    the schedule threshold convention, but implemented locally).
  - If the hex is missing/empty/unparseable, OR luminance is above a documented light
    threshold (e.g. > 0.7) such that the colour would be illegible on the white `bg-card`,
    return a readable dark fallback (a dark token / `#1a1a1a`-style value consistent with the
    card's normal text). Otherwise return the client hex unchanged.
Then update Row 2 (~L166-168): keep the `card.project.client?.name &&` guard, drop
`text-muted-foreground`, add a bold weight class (`font-bold` or `font-semibold`; keep
`text-xs`, add `leading-tight` to match the project-name row), and set
`style={{ color: resolveClientNameColor(card.project.client.color) }}`.
Extend the memo comparator (~L228) by adding, right after the existing client?.name line:
`prev.card.project.client?.color === next.card.project.client?.color &&`.
Do NOT modify CardDetailModal.tsx, any schedule file, avatar.tsx, types.ts, or any backend
file. Do NOT add or run any Prisma migration. Do NOT touch the DEVN-05 accepted lint line.
After editing, check LSP diagnostics and fix any new type error/missing import on the lines
you added.
  </action>
  <verify>
`cd frontend && npx tsc --noEmit` reports no new errors. Read the diff: only the Row-2 <p>,
the new local helper, and the comparator line changed; `text-muted-foreground` is gone from
the client-name <p>; no schedule import was added; no backend/schema/migration file changed
(`git status` shows only KanbanCard.tsx staged for this task).
  </verify>
  <done>
Client name renders bold + coloured by the resolved client colour; pale colours resolve to a
readable dark fallback; null/empty colour is safe; comparator guards client.color; frontend
type-checks with no new errors; change confined to KanbanCard.tsx.
  </done>
</task>
<task type="auto">
  <name>Add KanbanCard tests for bold + client colour, light-colour fallback, and missing-colour safety</name>
  <files>
    frontend/src/features/board/components/__tests__/KanbanCard.test.tsx
  </files>
  <action>
Add a new describe block (or extend the existing file) asserting the Row-2 client-name styling.
Add a small helper to grab the client-name <p> (query by its text, then read `.style.color`
and computed/inline font-weight — mirror the existing fallbackBgColors pattern that reads
`.style.backgroundColor`). Cases:
  (1) Mid/dark client colour (e.g. seed `client.color: '#3366ff'`): the rendered client-name
      element is bold (font-bold/font-semibold class present, or computed weight >= 600) AND
      its inline `style.color` resolves to the client hex (rgb(51,102,255)).
  (2) Pale client colour (e.g. `client.color: '#FFFACD'` / a near-white hex): the rendered
      client-name `style.color` is NOT the pale hex — it is the readable dark fallback (assert
      it differs from the pale colour / equals the documented dark fallback). Name still shows.
  (3) Missing/empty colour (e.g. `client: { id, name, color: '' }`): the name still renders
      (no crash) and uses the safe readable default.
Use makeCard(..., { client: { id, name, color } }) overrides via the existing projectOverrides
param. Keep all existing tests intact (test (d) must still find 'Acme Corp').
  </action>
  <verify>
`cd frontend && npx vitest run src/features/board/components/__tests__/KanbanCard.test.tsx`
passes — new cases plus all pre-existing avatar/memo cases green. `npx tsc --noEmit` clean.
  </verify>
  <done>
Three new assertions (bold+colour, light-colour fallback, missing-colour safety) pass; full
KanbanCard suite stays green; no type errors.
  </done>
</task>
</tasks>
<verification>
1. `git status` / diff: only frontend/src/features/board/components/KanbanCard.tsx and its
   __tests__/KanbanCard.test.tsx are modified. No backend file, no schema.prisma, no migrations
   dir entry, no CardDetailModal.tsx, no schedule file, no avatar.tsx.
2. `grep -n "features/schedule" frontend/src/features/board/components/KanbanCard.tsx` returns
   nothing (no schedule import / no getContrastColor reuse).
3. The client-name <p> in KanbanCard.tsx has a bold weight class, a `style={{ color: ... }}`,
   and no `text-muted-foreground`; the memo comparator contains the new client?.color guard.
4. `cd frontend && npx tsc --noEmit` reports no new errors.
5. `cd frontend && npx vitest run src/features/board/components/__tests__/KanbanCard.test.tsx`
   passes (existing + new cases).
</verification>
<success_criteria>
- Planner Kanban card preview shows the client name in bold and in the client's own colour.
- A pale client colour falls back to a readable dark colour on the white card (legibility
  guard implemented and asserted, not just commented).
- Missing/empty client colour renders the name safely with no crash.
- Card re-renders on a client colour edit (memo comparator guards client.color).
- Change is frontend-only: no backend change, no Prisma migration, no Schedule/CardDetailModal/
  avatar modification. Frontend tests + type-check are green.
</success_criteria>
<output>
10-01-SUMMARY.md
</output>
