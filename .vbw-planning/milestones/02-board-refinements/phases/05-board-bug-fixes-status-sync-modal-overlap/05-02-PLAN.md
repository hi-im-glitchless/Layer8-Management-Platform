---
phase: 5
plan: "02"
title: Card detail modal — stop close ✕ / pin icon overlap
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: fast
skills_used: []
files_modified:
  - frontend/src/features/board/components/CardDetailModal.tsx
must_haves:
  truths:
    - "The DialogTitle row reserves right padding so the 'manually placed' pin icon clears the shadcn close ✕ button (absolute right-4 top-4)"
    - "Both the close ✕ and the pin button remain fully visible and clickable; the project-name span still truncates/flexes as before"
    - "Only CardDetailModal.tsx is changed — the shared shadcn dialog primitive (dialog.tsx) is untouched; AssignmentModal and other dialogs are unaffected"
    - "No change to Phase-4 card layout, modal content, or behavior beyond the title-row padding"
  artifacts:
    - path: "frontend/src/features/board/components/CardDetailModal.tsx"
      provides: "DialogTitle with right padding clearing the close button"
      contains: "pr-8"
  key_links: []
---
<objective>
Fix Bug 2: in the card detail modal the shadcn close ✕ button (hardcoded `absolute right-4 top-4` in the shared `dialog.tsx` primitive) and the "manually placed" pin icon button (rightmost child of the `DialogTitle` flex row) overlap in the top-right corner. Add right padding to the `DialogTitle` className in `CardDetailModal.tsx` so the pin clears the ✕. Frontend-only, single file, no change to the shared primitive.
</objective>
<context>
@.vbw-planning/phases/05-board-bug-fixes-status-sync-modal-overlap/05-RESEARCH.md
@frontend/src/features/board/components/CardDetailModal.tsx
@frontend/src/components/ui/dialog.tsx

Rationale: Research verified the exact elements — the ✕ is appended by the shared `DialogContent` primitive at `absolute right-4 top-4` (dialog.tsx); the pin is the rightmost flex child of `DialogTitle` (~line 487 in CardDetailModal.tsx). dialog.tsx is included only to confirm the close-button position is owned by the shared primitive and must not be edited.
</context>
<tasks>
<task type="auto">
  <name>Add right padding to DialogTitle so the pin clears the close button</name>
  <files>
    frontend/src/features/board/components/CardDetailModal.tsx
  </files>
  <action>
On the `DialogTitle` element (~line 487), change the className from `flex items-center gap-2` to `flex items-center gap-2 pr-8` so the flex row's right end (where the pin button lives) clears the shadcn ✕ at `right-4`. Use `pr-8` (32px) as recommended by research; bump to `pr-10` only if `pr-8` still visually crowds the ✕. Do NOT edit the shared `dialog.tsx` primitive. Do NOT touch the color accent bar, the project-name `flex-1` span, the pin button's onClick/tooltip, or any other modal content. Verify the exact element matched is the `DialogTitle` that contains both the name span and the pin (not a different title elsewhere in the file).
  </action>
  <verify>
Run `cd frontend && npx tsc --noEmit` — clean. Grep `CardDetailModal.tsx` for `pr-8` and confirm it is on the `DialogTitle` line. Manually reason about layout: with `pr-8`, the pin button (h-4 w-4) sits ≥32px from the title's right edge, clearing the ✕ at 16px; both stay visible and clickable. If the project has no manual placement (`isManuallyPlaced` false), the extra padding is harmless (only the name span renders).
  </verify>
  <done>
DialogTitle has `pr-8`; close ✕ and pin no longer overlap; both clickable; shared dialog.tsx untouched; tsc clean.
  </done>
</task>
</tasks>
<verification>
1. `cd frontend && npx tsc --noEmit` clean.
2. Grep confirms `pr-8` on the `DialogTitle` line in CardDetailModal.tsx; `dialog.tsx` is unmodified (git diff shows only CardDetailModal.tsx changed).
3. Layout reasoning (or quick manual render): with a manually-placed card, the pin icon and the close ✕ are visually separated and both clickable; with a non-manually-placed card, the title still renders correctly with the extra right padding.
</verification>
<success_criteria>
- The card detail modal's close ✕ and "manually placed" pin icon no longer overlap; both remain visible and clickable.
- Only CardDetailModal.tsx changed; the shared shadcn dialog primitive and other dialogs (e.g. AssignmentModal) are unaffected.
- No regression to Phase-4 card/modal layout or content; tsc/build green.
</success_criteria>
<output>
05-02-SUMMARY.md
</output>
