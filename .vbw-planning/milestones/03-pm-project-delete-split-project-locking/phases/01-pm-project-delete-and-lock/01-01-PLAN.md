---
phase: 1
plan: "01"
title: PM Board Card Delete + Audit Trail + Confirmation Dialog
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: balanced
skills_used: []
files_modified:
  - backend/src/routes/board.ts
  - frontend/src/features/board/components/DeleteCardDialog.tsx
  - frontend/src/features/board/components/CardDetailModal.tsx
  - backend/src/services/__tests__/boardCardDelete.pm.test.ts
  - frontend/src/features/board/components/__tests__/DeleteCardDialog.test.tsx
forbidden_commands: []
must_haves:
  truths:
    - "DELETE /api/board/cards/:id is gated by requireRole('PM') so PM and ADMIN pass, NORMAL gets 403 (server authoritative, RBAC hierarchy NORMAL<PM<ADMIN)"
    - "Every successful board card delete writes a board.card.delete entry to the audit trail via logAuditEvent with cardId, projectName, and the acting user id"
    - "A confirmation AlertDialog is shown before the destructive delete; Cancel aborts with no mutation, Confirm calls useDeleteCard().mutate(cardId)"
    - "The PM-gated delete affordance renders in CardDetailModal for PM and ADMIN roles, and does not render for NORMAL role"
    - "Deleting a board card cascades to its comments/files/notifications but leaves the linked Project row and any referencing Assignment untouched (no schedule data loss)"
    - "NORMAL-role access is unchanged; no other board route guards are altered"
  artifacts:
    - path: "backend/src/routes/board.ts"
      provides: "PM-authorized + audited delete route"
      contains: "requireRole('PM')"
    - path: "backend/src/routes/board.ts"
      provides: "audit logging on delete"
      contains: "board.card.delete"
    - path: "frontend/src/features/board/components/DeleteCardDialog.tsx"
      provides: "confirmation AlertDialog component for hard-delete"
      contains: "AlertDialog"
    - path: "frontend/src/features/board/components/CardDetailModal.tsx"
      provides: "PM-gated Delete button + DeleteCardDialog wiring"
      contains: "DeleteCardDialog"
    - path: "backend/src/services/__tests__/boardCardDelete.pm.test.ts"
      provides: "delete cascade + project survival + audit test"
      contains: "board.card.delete"
    - path: "frontend/src/features/board/components/__tests__/DeleteCardDialog.test.tsx"
      provides: "dialog render/cancel/confirm test"
      contains: "useDeleteCard"
  key_links:
    - from: "CardDetailModal.tsx Delete button"
      to: "DeleteCardDialog.tsx"
      via: "deleteOpen state + setDeleteOpen(true)"
    - from: "DeleteCardDialog.tsx confirm"
      to: "frontend/src/features/board/hooks.ts useDeleteCard"
      via: "deleteCard.mutate(cardId)"
    - from: "DELETE /api/board/cards/:id handler"
      to: "backend/src/services/audit.ts logAuditEvent"
      via: "board.card.delete action with cardId/projectName/userId"
---
<objective>
Allow PM-role users (not just ADMIN) to hard-delete a board card (= the project), behind a confirmation AlertDialog, and write the delete to the audit trail. Covers ROADMAP Success Criteria 1 (PM delete authorized server-side), 2 (confirmation alert before delete; cancel aborts), and the 01-CONTEXT audit-trail decision. Server remains authoritative; the client affordance is advisory and gated by role. NORMAL-role access is unchanged.
</objective>
<context>
@.vbw-planning/phases/01-pm-project-delete-and-lock/01-RESEARCH.md
@.vbw-planning/phases/01-pm-project-delete-and-lock/01-CONTEXT.md
Rationale: 01-RESEARCH.md sections A, B, F, G, and "Files to Modify" carry exact paths/line numbers and the audit-gap finding. 01-CONTEXT.md carries the locked decisions (board card hard-delete opened to PM; AlertDialog not native confirm; audit as PM action). Pattern files to mirror (read before editing): backend audit/IP pattern in backend/src/routes/boardAdmin.ts (logAuditEvent + extractIp, action 'board.card.archive'); dialog pattern in frontend/src/features/board/components/ArchiveCardDialog.tsx; existing useDeleteCard in frontend/src/features/board/hooks.ts and boardApi.deleteCard in frontend/src/features/board/api.ts (both already exist — do NOT recreate); backend test pattern in backend/src/services/__tests__/deleteAssignmentOrphan.delete.test.ts and boardAutoMove.stopped.test.ts (real SQLite, unique-suffix seeding, withDbRetry, afterEach cleanup); frontend test pattern in frontend/src/features/board/components/__tests__/KanbanCard.test.tsx (vitest + @testing-library/react).
</context>
<tasks>
<task type="auto">
  <name>Open delete route to PM and add audit logging</name>
  <files>
    backend/src/routes/board.ts
  </files>
  <action>
In the DELETE /cards/:id handler (~lines 203-217): change requireRole('ADMIN') to requireRole('PM'). Add audit logging mirroring boardAdmin.ts: before deleting, fetch the card (boardService.getCard or a Prisma findUnique selecting id + project { name }) to capture projectName; after the successful boardService.deleteCard(id), call logAuditEvent with action 'board.card.delete' and details { cardId: id, projectName, userId: <acting user id> } plus ip via the same extractIp pattern used in boardAdmin.ts. Add the import for logAuditEvent from '../services/audit.js' (match the existing import extension convention in this file). Keep the existing emitBoardInvalidate('cards') call and the P2025 -> 404 handling intact. Do not alter any other route in this file. If the pre-fetch returns null, return 404 before deleting (so audit never logs a phantom delete).
  </action>
  <verify>
LSP diagnostics clean on board.ts (no missing imports / type errors). grep shows requireRole('PM') on the delete route and board.card.delete present. Confirm no other requireRole guard in the file changed (git diff scoped to the delete handler + import line).
  </verify>
  <done>
Delete route is requireRole('PM'), pre-fetches projectName, logs board.card.delete audit event with cardId/projectName/userId, preserves invalidate + 404 behavior. One commit: feat(board): allow PM to delete card with audit logging.
  </done>
</task>
<task type="auto">
  <name>Create DeleteCardDialog confirmation component</name>
  <files>
    frontend/src/features/board/components/DeleteCardDialog.tsx
  </files>
  <action>
New file mirroring ArchiveCardDialog.tsx. Use AlertDialog primitives from '@/components/ui/alert-dialog'. Props: { cardId: string; projectName: string; open: boolean; onOpenChange: (open: boolean) => void; onDeleted?: () => void }. Call useDeleteCard() from '../hooks' internally. Title makes clear this is a permanent delete (not archive). Description/warning text: "This permanently deletes the card and all attached comments, notes, and files. The linked schedule assignments are not affected. This cannot be undone." Cancel action closes the dialog and performs no mutation. The destructive Confirm action calls deleteCard.mutate(cardId); on success call onDeleted?.() then onOpenChange(false). Disable the confirm button while deleteCard.isPending. Include projectName in the visible text so the user sees which project they are deleting.
  </action>
  <verify>
LSP diagnostics clean. Component imports AlertDialog and useDeleteCard. No native confirm() used.
  </verify>
  <done>
DeleteCardDialog.tsx exists, AlertDialog-based, warning text present, Cancel aborts, Confirm mutates via useDeleteCard. One commit: feat(board): add DeleteCardDialog confirmation component.
  </done>
</task>
<task type="auto">
  <name>Wire PM-gated Delete button + dialog into CardDetailModal</name>
  <files>
    frontend/src/features/board/components/CardDetailModal.tsx
  </files>
  <action>
Wire the existing dead canDelete var (role === 'ADMIN' || role === 'PM', ~line 483) to UI. Add const [deleteOpen, setDeleteOpen] = useState(false) (mirroring archiveOpen). Import DeleteCardDialog and ensure Trash2 from lucide-react is imported. In DialogFooter, add a destructive Delete button guarded by canDelete (label "Delete card", Trash2 icon, mr-auto when archive button is absent) that calls setDeleteOpen(true). Keep the existing canArchive Archive button (ADMIN-only) so ADMIN sees both Archive and Delete, PM sees only Delete, NORMAL sees neither. Render <DeleteCardDialog cardId={card.id} projectName={<card project name field>} open={deleteOpen} onOpenChange={setDeleteOpen} onDeleted={() => onOpenChange(false)} /> below the existing ArchiveCardDialog. Do not weaken the canArchive guard.
  </action>
  <verify>
LSP diagnostics clean. grep shows DeleteCardDialog and deleteOpen in the file. canArchive guard unchanged.
  </verify>
  <done>
Delete button renders for PM+ADMIN only, opens DeleteCardDialog, onDeleted closes the modal; Archive stays ADMIN-only. One commit: feat(board): wire PM delete affordance into card detail modal.
  </done>
</task>
<task type="auto">
  <name>Backend test: delete cascade, project survival, audit entry</name>
  <files>
    backend/src/services/__tests__/boardCardDelete.pm.test.ts
  </files>
  <action>
New vitest file following deleteAssignmentOrphan.delete.test.ts conventions (real SQLite dev DB, unique-suffix seed rows, withDbRetry, afterEach cleanup). Seed Client + Project + BoardCard + BoardComment + BoardFile + an Assignment referencing the project. Exercise the delete path (call the route handler or boardService.deleteCard plus the audit call as the route performs it — match however the route is structured so the test covers the audited delete behavior). Assert: BoardCard row gone; BoardComment and BoardFile rows gone (cascade); Project row still exists; Assignment row still exists with its projectId/splitProjectId FK intact. Assert an audit entry with action 'board.card.delete' exists referencing the deleted cardId and the acting userId. Clean up all seeded rows (and any audit rows created) in afterEach.
  </action>
  <verify>
Run: cd backend && npx vitest run src/services/__tests__/boardCardDelete.pm.test.ts. All assertions pass. No leftover seeded rows after run.
  </verify>
  <done>
Test passes covering cascade, project/assignment survival, and audit entry. One commit: test(board): cover PM card delete cascade and audit entry.
  </done>
</task>
<task type="auto">
  <name>Frontend test: DeleteCardDialog render / cancel / confirm</name>
  <files>
    frontend/src/features/board/components/__tests__/DeleteCardDialog.test.tsx
  </files>
  <action>
New vitest + @testing-library/react test following KanbanCard.test.tsx conventions. Mock useDeleteCard (e.g., vi.mock '../../hooks') returning a mutate spy. Tests: (1) renders the warning text and projectName when open; (2) clicking Cancel calls onOpenChange(false) and does NOT call mutate; (3) clicking Confirm calls the mutate spy with the cardId. Wrap with any required providers (QueryClientProvider if the component reaches react-query directly rather than via the mocked hook — prefer mocking the hook so no real client is needed).
  </action>
  <verify>
Run: cd frontend && npx vitest run src/features/board/components/__tests__/DeleteCardDialog.test.tsx. All three tests pass.
  </verify>
  <done>
Dialog render/cancel/confirm behavior verified. One commit: test(board): cover DeleteCardDialog confirm and cancel.
  </done>
</task>
</tasks>
<verification>
1. cd backend && npx vitest run src/services/__tests__/boardCardDelete.pm.test.ts passes.
2. cd frontend && npx vitest run src/features/board/components/__tests__/DeleteCardDialog.test.tsx passes.
3. grep -n "requireRole('PM')" and "board.card.delete" in backend/src/routes/board.ts both match within the delete handler.
4. LSP diagnostics clean on board.ts, DeleteCardDialog.tsx, CardDetailModal.tsx.
5. git diff confirms no board route guard other than the delete route was changed; canArchive remains ADMIN-only.
</verification>
<success_criteria>
- PM (and ADMIN) can delete a board card; server authorizes via requireRole('PM'); NORMAL receives 403 (ROADMAP SC1, SC5).
- A confirmation AlertDialog precedes deletion; Cancel makes no change, Confirm deletes (ROADMAP SC2).
- Every successful delete writes a board.card.delete audit entry (01-CONTEXT audit decision).
- Delete cascades to comments/files/notifications but preserves the Project row and referencing Assignments (no schedule data loss; ROADMAP SC5 invariants).
- PM-gated Delete affordance renders for PM/ADMIN only; NORMAL access unchanged.
</success_criteria>
<output>
01-SUMMARY.md
</output>
