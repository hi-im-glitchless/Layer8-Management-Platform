---
phase: 1
plan: "02"
title: Split-Cell Lock Toggle + Assignment Modal Lock Control
type: execute
wave: 1
depends_on: []
cross_phase_deps: []
autonomous: true
effort_override: balanced
skills_used: []
files_modified:
  - frontend/src/features/schedule/components/AssignmentCell.tsx
  - frontend/src/features/schedule/components/AssignmentModal.tsx
  - frontend/src/features/schedule/components/__tests__/AssignmentCell.split-lock.test.tsx
  - frontend/src/features/schedule/components/__tests__/AssignmentModal.lock.test.tsx
forbidden_commands: []
must_haves:
  truths:
    - "SplitCell exposes a clickable lock/unlock affordance that calls onLockToggle, mirroring the non-split cell; the whole assignment is the unit of locking (single isLocked boolean, existing POST /assignments/:id/lock, no backend change)"
    - "onLockToggle is threaded from the AssignmentCell exported component into SplitCell (previously silently dropped)"
    - "Split-cell lock affordance follows the three cases: canEdit && isLocked -> visible clickable lock; canEdit && !isLocked -> hover-visible clickable lock; !canEdit && isLocked -> static icon (no button)"
    - "The lock button onClick calls e.stopPropagation() so cell onCellClick does not fire"
    - "AssignmentModal shows a lock/unlock toggle button (footer, near Delete) driven by assignment.isLocked read from the prop (not local state); clicking it calls useToggleLock().mutate(assignment.id)"
    - "While assignment.isLocked is true, all editable form fields, the Save button, and the Delete button are disabled, but the lock-toggle button itself remains enabled so the user can unlock"
    - "Existing backend lock/edit/delete guards and NORMAL-role access are unchanged (frontend-only plan)"
  artifacts:
    - path: "frontend/src/features/schedule/components/AssignmentCell.tsx"
      provides: "clickable split-cell lock toggle + onLockToggle threading"
      contains: "onLockToggle"
    - path: "frontend/src/features/schedule/components/AssignmentModal.tsx"
      provides: "modal lock toggle + locked-field disabling"
      contains: "useToggleLock"
    - path: "frontend/src/features/schedule/components/__tests__/AssignmentCell.split-lock.test.tsx"
      provides: "split-cell lock visibility + callback test"
      contains: "onLockToggle"
    - path: "frontend/src/features/schedule/components/__tests__/AssignmentModal.lock.test.tsx"
      provides: "modal locked/unlocked disabling + toggle test"
      contains: "useToggleLock"
  key_links:
    - from: "AssignmentCell exported component"
      to: "SplitCell"
      via: "onLockToggle prop passed through"
    - from: "SplitCell lock button"
      to: "ScheduleGrid handleLockToggle -> useToggleLock"
      via: "onLockToggle(e) -> POST /assignments/:id/lock"
    - from: "AssignmentModal lock toggle button"
      to: "frontend/src/features/schedule/hooks.ts useToggleLock"
      via: "toggleLock.mutate(assignment.id)"
---
<objective>
Surface the lock control on split (two-project) cells and add a lock control to the assignment-edit modal. Covers ROADMAP Success Criteria 3 (split projects display the lock control, consistent with non-split) and 4 (lock control present in the assignment-edit modal). The whole assignment is the unit of locking (single isLocked boolean). Frontend-only: backend toggleLock service, POST /assignments/:id/lock route (requireRole('PM')), and the updateAssignment/deleteAssignment locked guards all already exist and must remain unchanged. NORMAL-role access is unchanged.
</objective>
<context>
@.vbw-planning/phases/01-pm-project-delete-and-lock/01-RESEARCH.md
@.vbw-planning/phases/01-pm-project-delete-and-lock/01-CONTEXT.md
Rationale: 01-RESEARCH.md sections C (backend lock — already complete, do not modify), D (the SplitCell gap with exact line numbers and the three-case clickable pattern from the non-split branch), E (modal lock control: hook import, isLocked derivation, full list of fields to disable, toggle-button placement, the "toggle stays enabled while locked" rule), and G/Edge-cases 3-6 (Delete button must also be disabled while locked; isLocked is read from the prop; SplitHalf needs no toggle). 01-CONTEXT.md carries the locked decisions (whole-assignment lock; per-half locking rejected; disable Save/fields while locked, unlock re-enables). Mirror the existing non-split lock-toggle markup in AssignmentCell.tsx (canEdit/isLocked branches) for the split version. useToggleLock already exists in frontend/src/features/schedule/hooks.ts (do not recreate). Frontend test pattern: frontend/src/features/board/components/__tests__/KanbanCard.test.tsx (vitest + @testing-library/react; DndContext wrapper if the cell requires droppable/draggable context).
</context>
<tasks>
<task type="auto">
  <name>Add clickable lock toggle to SplitCell and thread onLockToggle</name>
  <files>
    frontend/src/features/schedule/components/AssignmentCell.tsx
  </files>
  <action>
(1) Add onLockToggle?: (e: React.MouseEvent) => void to the SplitCell prop interface (~lines 227-249). (2) In the AssignmentCell exported component's isSplit branch (~lines 421-437), pass onLockToggle={onLockToggle} into <SplitCell>. (3) Replace the static lock icon block in SplitCell (~lines 302-304) with the three-case clickable pattern mirroring the non-split branch (~lines 461-481): canEdit && isLocked -> always-visible clickable lock button; canEdit && !isLocked -> hover-visible clickable lock button (opacity-0 group-hover:opacity-60 hover:!opacity-100); !canEdit && isLocked -> keep the existing static <Lock> icon (no button). The SplitCell wrapper div MUST carry the `group` CSS class so the hover-reveal lock button's `group-hover:opacity-60` resolves, exactly as the non-split branch wrapper already does — this is required, not optional; without `group` on the wrapper the hover reveal silently never triggers. Keep the absolute top-right overlay positioning. Each button's onClick must call e.stopPropagation() then onLockToggle?.(e) so the cell's onCellClick does not fire. Keep the existing text-muted-foreground color for the overlay (planner discretion noted in research). Do not change SplitHalf and do not add any per-half lock. [resolved-by-amendment: DEVN-01a — the delivered code in AssignmentCell.tsx correctly adds `group` to the SplitCell wrapper div; the original plan omitted this explicit step. The delivered code is correct and is retained; this amendment documents the actual approach, nothing is reverted.]
  </action>
  <verify>
LSP diagnostics clean. grep shows onLockToggle in SplitCell props, in the SplitCell invocation, and in the new button onClick handlers. Non-split lock markup unchanged.
  </verify>
  <done>
SplitCell renders the clickable lock affordance for all three (canEdit,isLocked) cases, onLockToggle is threaded through, stopPropagation present. One commit: feat(schedule): add lock toggle to split assignment cells.
  </done>
</task>
<task type="auto">
  <name>Add lock toggle + locked-field disabling to AssignmentModal</name>
  <files>
    frontend/src/features/schedule/components/AssignmentModal.tsx
  </files>
  <action>
(1) Import useToggleLock from '../hooks'; add Lock, Unlock to the lucide-react import (alongside ExternalLink, Trash2). (2) Inside the component add const toggleLock = useToggleLock(). (3) Derive const isLocked = isEdit && !!assignment?.isLocked (read from the prop, not local state). (4) Add disabled={isLocked} to all editable inputs/selects/buttons: projectName Input, splitProjectName Input, ClientSelect (add/forward a disabled prop), ColorPalette (add/forward a disabled prop), each status button, TagSelector tag buttons, and the split-toggle Checkbox. (5) Save button: disabled={upsertMutation.isPending || isLocked}. (6) Delete button: disabled={deleteMutation.isPending || isLocked} (per edge case 3, prevents a 409). (7) Add a lock/unlock toggle Button in the DialogFooter after the Delete button (visible only when isEdit && assignment): variant outline, size sm, onClick={() => toggleLock.mutate(assignment.id)}, disabled={toggleLock.isPending} — this button must NOT be disabled by isLocked. Label/icon: assignment.isLocked ? (Unlock icon + 'Unlock') : (Lock icon + 'Lock'). If ClientSelect/ColorPalette do not already accept a disabled prop, add the prop and forward it to the underlying control (minimal change, no behavior change when undefined/false). After unlock succeeds, useToggleLock invalidates ['schedule','assignments'] so the parent re-passes assignment.isLocked: false — no extra local state needed.
  </action>
  <verify>
LSP diagnostics clean on AssignmentModal.tsx (and ClientSelect/ColorPalette if a disabled prop was added). grep shows useToggleLock, Unlock, and disabled={isLocked} present. The lock-toggle button is not gated by isLocked.
  </verify>
  <done>
Modal shows a Lock/Unlock toggle reflecting assignment.isLocked; while locked all fields + Save + Delete are disabled but the toggle stays enabled and unlocks. One commit: feat(schedule): add lock control and locked-field disabling to assignment modal.
  </done>
</task>
<task type="auto">
  <name>Frontend test: SplitCell lock visibility and callback</name>
  <files>
    frontend/src/features/schedule/components/__tests__/AssignmentCell.split-lock.test.tsx
  </files>
  <action>
New vitest + @testing-library/react test following KanbanCard.test.tsx (wrap in DndContext if the cell needs droppable/draggable context). The `Assignment` type import in this test file MUST use `../../types` (NOT `../types`), because the `__tests__/` subdirectory sits one level deeper than the component directory — `../types` resolves to a non-existent path and fails tsc. Render AssignmentCell with a split assignment (splitProjectId set) across the (canEdit, isLocked) combinations and assert: canEdit=true & isLocked=true -> a clickable lock button is present and clicking it calls the onLockToggle spy; canEdit=false & isLocked=true -> a static lock icon renders and NO clickable lock button is present; (hover-only visibility for canEdit=true & isLocked=false relies on CSS group-hover which jsdom cannot evaluate — assert the button element exists in the DOM rather than its visual visibility, and add a brief comment that hover reveal is a visual smoke check). Verify clicking the lock button does not also trigger onCellClick (stopPropagation): pass an onCellClick spy and assert it is not called when the lock button is clicked.
  </action>
  <verify>
Run: cd frontend && npx vitest run src/features/schedule/components/__tests__/AssignmentCell.split-lock.test.tsx. All cases pass.
  </verify>
  <done>
Split-cell lock visibility for all (canEdit,isLocked) cases verified, callback fired, stopPropagation confirmed. One commit: test(schedule): cover split-cell lock toggle visibility and callback. [resolved-by-amendment: DEVN-01b — the delivered AssignmentCell.split-lock.test.tsx correctly imports the `Assignment` type from `../../types`; the original plan did not specify the import depth. The delivered import is correct and is retained; this amendment documents the actual approach, nothing is reverted.]
  </done>
</task>
<task type="auto">
  <name>Frontend test: AssignmentModal lock state and toggle</name>
  <files>
    frontend/src/features/schedule/components/__tests__/AssignmentModal.lock.test.tsx
  </files>
  <action>
New vitest + @testing-library/react test. The `Assignment` type import in this test file MUST use `../../types` (NOT `../types`), because the `__tests__/` subdirectory sits one level deeper than the component directory — `../types` resolves to a non-existent path and fails tsc. Mock useToggleLock (and useUpsertAssignment/useDeleteAssignment as needed) to return spies; wrap in QueryClientProvider only if required by un-mocked hooks. Tests: (1) isEdit=true with assignment.isLocked=true -> Save button disabled, project name Input disabled, lock-toggle button shows 'Unlock' label and is itself NOT disabled; (2) isEdit=true with assignment.isLocked=false -> Save button enabled, lock-toggle button shows 'Lock' label; (3) clicking the toggle calls the useToggleLock mutate spy with assignment.id. Optionally assert the Delete button is disabled when isLocked=true.
  </action>
  <verify>
Run: cd frontend && npx vitest run src/features/schedule/components/__tests__/AssignmentModal.lock.test.tsx. All tests pass.
  </verify>
  <done>
Modal locked/unlocked disabling, toggle label, and mutate call verified. One commit: test(schedule): cover assignment modal lock control behavior. [resolved-by-amendment: DEVN-01b — the delivered AssignmentModal.lock.test.tsx correctly imports the `Assignment` type from `../../types`; the original plan did not specify the import depth. The delivered import is correct and is retained; this amendment documents the actual approach, nothing is reverted.]
  </done>
</task>
</tasks>
<verification>
1. cd frontend && npx vitest run src/features/schedule/components/__tests__/AssignmentCell.split-lock.test.tsx passes.
2. cd frontend && npx vitest run src/features/schedule/components/__tests__/AssignmentModal.lock.test.tsx passes.
3. grep -n "onLockToggle" in AssignmentCell.tsx matches in SplitCell props, the SplitCell invocation, and the new button handlers.
4. grep -n "useToggleLock" and "disabled={isLocked}" in AssignmentModal.tsx both match; the lock-toggle button is not gated by isLocked.
5. LSP diagnostics clean on AssignmentCell.tsx and AssignmentModal.tsx (and any select/palette component given a new disabled prop).
6. git diff touches only schedule frontend files — no backend changes (lock backend already complete).
</verification>
<success_criteria>
- Split projects display a clickable lock/unlock control consistent with non-split cells; the whole assignment locks/unlocks via the existing PM lock route (ROADMAP SC3).
- The assignment-edit modal shows a lock/unlock toggle reflecting assignment.isLocked (ROADMAP SC4).
- While locked, the modal's fields, Save, and Delete are disabled while the toggle stays enabled to allow unlock (01-CONTEXT lock-behavior decision; edge case 3).
- Existing backend lock/edit/delete guards and NORMAL-role access are preserved (no backend changes; ROADMAP SC5 invariants).
</success_criteria>
<output>
02-SUMMARY.md
</output>
