---
phase: 01.1-ui-ux-visual-polish
plan: 03
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/admin/UserManagement.tsx
  - frontend/src/components/admin/AuditLogViewer.tsx
  - frontend/src/components/admin/SessionManagement.tsx
autonomous: false

must_haves:
  truths:
    - "Sidebar has roomy Notion-like spacing with comfortable padding on nav items"
    - "Active sidebar item shows background highlight AND left blue border accent"
    - "Sidebar hover states show background highlight with smooth transition"
    - "Logo in sidebar blends seamlessly and hides completely when collapsed"
    - "Admin tables use hover-only row highlighting with no striped rows"
    - "All table rows have consistent hover highlight effect"
  artifacts:
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Notion-like sidebar with active border accent and logo integration"
      contains: "border-l"
    - path: "frontend/src/components/admin/UserManagement.tsx"
      provides: "Clean table with hover-only row highlighting"
      contains: "hover:bg-muted"
    - path: "frontend/src/components/admin/AuditLogViewer.tsx"
      provides: "Audit log table with hover-only highlighting"
      contains: "hover:bg-muted"
    - path: "frontend/src/components/admin/SessionManagement.tsx"
      provides: "Session table with hover-only highlighting"
      contains: "hover:bg-muted"
  key_links:
    - from: "frontend/src/components/layout/Sidebar.tsx"
      to: "frontend/src/index.css"
      via: "sidebar-accent and accent CSS variables"
      pattern: "sidebar-accent|border-accent|border-blue"
    - from: "frontend/src/components/admin/UserManagement.tsx"
      to: "frontend/src/components/ui/table.tsx"
      via: "TableRow component with hover classes"
      pattern: "hover:bg-muted"
---

<objective>
Polish the sidebar navigation and admin panel tables to match the new design direction.

Purpose: The sidebar is the primary navigation element — it must feel roomy and polished with clear active state indicators per user decision. Admin tables currently use striped rows which the user explicitly wants replaced with hover-only highlighting. This plan covers component-level refinements that depend on the color palette from Plan 01.

Output: Restyled Sidebar.tsx with Notion-like spacing and blue border active state, restyled admin tables without stripes.
</objective>

<execution_context>
@/home/glitchless/.claude/get-shit-done/workflows/execute-plan.md
@/home/glitchless/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-ui-ux-visual-polish-dark-theme-logo-integration-login-aesthetics/01.1-CONTEXT.md
@.planning/phases/01.1-ui-ux-visual-polish-dark-theme-logo-integration-login-aesthetics/01.1-RESEARCH.md
@.planning/phases/01.1-ui-ux-visual-polish-dark-theme-logo-integration-login-aesthetics/01.1-01-SUMMARY.md
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/components/admin/UserManagement.tsx
@frontend/src/components/admin/AuditLogViewer.tsx
@frontend/src/components/admin/SessionManagement.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish sidebar with Notion-like spacing and active state indicators</name>
  <files>frontend/src/components/layout/Sidebar.tsx</files>
  <action>
Update `frontend/src/components/layout/Sidebar.tsx` to implement the user's locked decisions for sidebar styling. Preserve ALL existing logic: navigation groups, collapsed state, localStorage persistence, admin visibility filtering, theme-based logo switching.

**Spacing changes (Notion-like, roomy):**
- Outer nav padding: Change from `py-4` to `py-6 px-3`
- Group label: Change `px-3 mb-2` to `px-3 mb-3`, add `font-semibold` alongside existing `font-medium`
- Navigation item container: Change `space-y-1 px-2` to `space-y-1.5` (px handled by individual items)
- NavLink items: Change padding from `px-3 py-2` to `px-4 py-2.5` for more breathing room
- Change `rounded-md` to `rounded-lg` on NavLinks for softer look
- Group separator: Change `my-2 mx-3` to `my-4 mx-3` for more space between groups, or replace `<Separator>` with margin only (use `mb-6` on each group div for Notion-like feel without separator lines)

**Active state (LOCKED — must have BOTH background AND left border):**
- Active NavLink: `bg-sidebar-accent text-sidebar-accent-foreground border-l-3 border-blue-500` (3px left border in accent blue)
- Add `pl-[13px]` to active state to compensate for the 3px border (keeps text aligned with non-active items that have `px-4` = 16px padding-left; 3px border + 13px padding = 16px visual alignment)
- Non-active items: `text-sidebar-foreground/70`

**Hover state:**
- Add `hover:bg-sidebar-accent/50 hover:text-sidebar-accent-foreground` for non-active items
- Add `transition-all duration-200` for smooth transitions (instead of just `transition-colors`)

**Logo area:**
- Keep the existing theme-based logo switching logic (`resolvedTheme === 'dark'`)
- Keep logo hidden when collapsed (existing `!collapsed && ...` check)
- When collapsed, keep the red/accent "8" character — but change `text-accent` to `text-blue-500` to match the new cool blue accent
- Add `py-2` to the logo container for slightly more vertical breathing room: Change `h-14` to `h-16`
- Remove any shadow or border treatment on the logo image. Keep only: `h-8 w-auto object-contain`

**Do NOT change:**
- The `navigationGroups` data structure
- The `collapsed`/`setCollapsed` state management
- The `localStorage` persistence logic
- The `useAuth` hook usage or admin visibility filtering
- The collapse toggle button at the bottom
  </action>
  <verify>
Run `cd /home/glitchless/Documents/Layer8/frontend && npx tsc --noEmit` to verify no TypeScript errors. Run `npm run build` to verify build. Grep Sidebar.tsx for "border-l" to confirm left border accent is present. Grep for "border-blue" to confirm blue color used.
  </verify>
  <done>
Sidebar has Notion-like roomy spacing (px-4 py-2.5 on items, mb-6 between groups). Active nav item shows both background highlight AND left 3px blue border. Hover states use accent background with smooth transitions. Logo area has breathing room. Collapsed "8" icon uses blue accent. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove table row striping and add hover-only highlighting</name>
  <files>
    frontend/src/components/admin/UserManagement.tsx
    frontend/src/components/admin/AuditLogViewer.tsx
    frontend/src/components/admin/SessionManagement.tsx
  </files>
  <action>
Update all three admin panel table components to replace striped row styling with hover-only highlighting per user decision.

**UserManagement.tsx:**
- Find the `<TableRow>` inside `<TableBody>` that maps over users (line ~110)
- Change: `className={index % 2 === 0 ? 'bg-muted/50' : ''}`
- To: `className="hover:bg-muted/50 transition-colors"`
- This removes alternating background colors and adds a hover highlight instead

**AuditLogViewer.tsx:**
- Find any `<TableRow>` elements in the table body
- If there is striped styling (check for `index % 2` pattern or alternating classes), remove it
- Add `className="hover:bg-muted/50 transition-colors"` to each data TableRow
- If no striping exists, just ensure hover highlighting is present

**SessionManagement.tsx:**
- Same pattern: find table body rows, remove any striped styling
- Add `className="hover:bg-muted/50 transition-colors"` to each data TableRow

For all three files:
- Do NOT change any business logic, data fetching, mutations, or event handlers
- Do NOT change table headers, column structure, or cell content
- Only modify the `className` prop on `<TableRow>` elements in the table body
- Keep existing `key` props and other attributes
  </action>
  <verify>
Run `cd /home/glitchless/Documents/Layer8/frontend && npx tsc --noEmit` for TypeScript verification. Run `npm run build`. Grep all three files for "index % 2" — should return NO matches (striping removed). Grep for "hover:bg-muted" — should return matches in all three files.
  </verify>
  <done>
All three admin tables (UserManagement, AuditLogViewer, SessionManagement) use hover-only row highlighting. No striped/alternating row colors remain. Each table row has smooth hover transition. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all UI polish changes</name>
  <files>none — visual verification only</files>
  <action>
This is a human verification checkpoint covering all three plans (01, 02, 03) of Phase 01.1. Start dev servers and verify the complete visual polish in the browser.

Complete UI visual polish across the entire application:
- Deep dark theme with cool blue accent (replacing old red)
- Clean light theme with matching blue direction
- Atmospheric login page with gradient + glassmorphism
- Onboarding wizard with matching atmospheric styling
- Notion-like sidebar with active border + hover states
- Gradient primary and destructive buttons
- Hover-only table rows in admin panel
- Favicon from Layer8 logo
  </action>
  <verify>
1. Start the dev servers: `cd /home/glitchless/Documents/Layer8/backend && npm run dev` and `cd /home/glitchless/Documents/Layer8/frontend && npm run dev`
2. Visit http://localhost:5173/login
   - Verify: Gradient left side with blue/purple atmospheric tones
   - Verify: Glassmorphism form card (semi-transparent with blur)
   - Verify: Logo blends into gradient without bounding box
   - Verify: Favicon shows Layer8 logo in browser tab (not Vite icon)
3. Log in (admin / your password)
   - If first login: Verify onboarding wizard has matching dark gradient + glass styling
4. Check sidebar (should be in dark theme by default):
   - Verify: Roomy spacing between nav items
   - Verify: Active item has blue left border AND background highlight
   - Verify: Hover on non-active items shows background highlight
   - Verify: Logo visible when expanded, hidden when collapsed
5. Toggle to light theme (theme toggle in header):
   - Verify: Clean white with blue accent — looks polished and cohesive
   - Verify: Buttons use blue accent, not red
   - Verify: Sidebar active state still visible with blue border
6. Navigate to Admin Panel:
   - Verify: User table has hover highlight only (no alternating stripes)
   - Verify: Session management table same
   - Verify: Audit log table same
7. Check any buttons labeled with primary action: Should show subtle blue gradient

Resume signal: Type "approved" or describe specific issues to fix
  </verify>
  <done>
User has visually confirmed: dark theme is deep and dramatic with cool blue accent, light theme is clean and cohesive, login page feels atmospheric with glassmorphism, sidebar is roomy with blue active state border, tables are clean with hover-only highlighting, favicon shows in browser tab. Both themes look polished and professional.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes in frontend directory
2. No `index % 2` striping patterns remain in any admin table component
3. Sidebar.tsx contains `border-l` for active state
4. All table components contain `hover:bg-muted` for row highlighting
5. Visual verification confirms atmospheric login, polished sidebar, and clean tables
</verification>

<success_criteria>
- Sidebar is roomy with clear Notion-like spacing
- Active nav item has both background highlight and left blue border
- Logo switches per theme and hides on collapse
- Admin tables use hover-only highlighting, no stripes
- User approves visual appearance in both light and dark themes
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-ux-visual-polish-dark-theme-logo-integration-login-aesthetics/01.1-03-SUMMARY.md`
</output>
