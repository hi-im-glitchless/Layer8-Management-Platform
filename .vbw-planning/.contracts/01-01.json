{
  "phase_id": "phase-01",
  "plan_id": "phase-01-plan-01",
  "phase": 1,
  "plan": 1,
  "objective": "Planner Client-First Name Order (card + detail modal)",
  "task_ids": [],
  "task_count": 0,
  "allowed_paths": [],
  "forbidden_paths": [],
  "depends_on": [],
  "must_haves": [
    "KanbanCard row 1 renders the CLIENT name with the headline classes `text-lg font-semibold leading-tight line-clamp-2`; row 2 renders the PROJECT name with `text-sm font-bold leading-tight`.",
    "The headline text is the chained-OR fallback `card.project.client?.name || card.project.name || '(No project)'` (Option A from research §1 — the codebase's established `|| '(No project)'` idiom). No new placeholder string such as '(No client)' is introduced.",
    "When `card.project.client?.name` is falsy the row-2 project line is NOT rendered — the project name appears exactly once on the card (as the headline), never twice, and the first line is never blank.",
    "The pin `<Pin className=\\\"h-3 w-3 shrink-0 text-muted-foreground\\\" />` stays inside the SAME `div.flex.items-start.justify-between.gap-1` wrapper as the headline `<p>` (i.e. it moves with row 1, staying top-right) and its condition `card.stageLockedBy && card.stageLockedBy !== 'auto'` is unchanged.",
    "The client name carries NO inline colour and is not muted: the headline `<p>` has no `style` prop, so `el.style.color === ''` holds, and its className does not contain `text-muted-foreground`. The Phase-10 legibility rationale comment (KanbanCard.tsx:167-169) moves with the client name to row 1 rather than being deleted.",
    "The `memo` comparator (KanbanCard.tsx:223-241) is byte-for-byte unchanged. VERIFIED during planning: it already compares BOTH `prev.card.project.name === next.card.project.name` (L228) and `prev.card.project.client?.name === next.card.project.client?.name` (L234); the reorder reads no new field, so its coverage is already complete. Do NOT edit it.",
    "CardDetailModal `DialogTitle` text is `project.client?.name || project.name || '(No project)'`; a project-name line follows it inside `DialogHeader`, rendered only when `project.client?.name` is truthy.",
    "The duplicate client `<span>` in the modal's 'Client + tags' row (CardDetailModal.tsx:546) is REMOVED and that row's guard collapses to `project.tags.length > 0` — the client name renders exactly once in the modal. The tag `<span>` markup itself is unchanged.",
    "The three tests in `describe('KanbanCard client name styling')` keep their `expect(el.style.color).toBe('')` assertion verbatim (the no-inline-colour regression guard); only the weight expectation changes from `font-bold` to `font-semibold`, plus test (1) keeps `not.toContain('text-muted-foreground')`.",
    "`CardDetailModal.test.tsx` gains header-order coverage (it currently asserts nothing about `DialogTitle`), including the clientless fallback case.",
    "No file outside the four in `files_modified` is changed. Specifically `AssignmentCell.tsx`, `exportHtml.ts` and dashboard `ProjectCard.tsx` are untouched, and there are no backend/schema/API changes.",
    "The pre-existing accepted ESLint finding on `KanbanCard.tsx` (DEVN-05, `react-refresh/only-export-components`, CONCERNS.md:30) is NOT fixed, NOT suppressed, and NOT treated as a regression from this diff.",
    "path: \"frontend/src/features/board/components/KanbanCard.tsx",
    "path: \"frontend/src/features/board/components/CardDetailModal.tsx",
    "path: \"frontend/src/features/board/components/__tests__/KanbanCard.test.tsx",
    "path: \"frontend/src/features/board/components/__tests__/CardDetailModal.test.tsx",
    "from: \"frontend/src/features/board/components/KanbanCard.tsx",
    "from: \"frontend/src/features/board/components/CardDetailModal.tsx"
  ],
  "verification_checks": [],
  "max_token_budget": 50000,
  "timeout_seconds": 600,
  "contract_hash": "2c3bd54cdc1dfa5f0a93377fdec03f3d754e2797f23f696df47ae58c03bed409"
}
