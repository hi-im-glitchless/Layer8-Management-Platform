[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_functions = "test_*"
addopts = "-v --tb=short"
markers = [
    "unit: marks tests as unit tests (no external dependencies)",
    "requires_spacy: marks tests that require spaCy models",
]
